---
phase: 03-vue-frontend
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/App.vue
  - src/components/MainLayout.vue
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "App.vue wraps MainLayout in NDialogProvider, NMessageProvider, NNotificationProvider so useMessage/useDialog/useNotification work in all child components"
    - "MainLayout renders a dual-panel NLayout with left NLayoutSider (seed panel, min 280px) and right NLayoutContent (queue + batch)"
    - "A draggable divider between panels allows resizing with clamp (250px-70% viewport)"
    - "All three composable subscriptions are initialized in MainLayout's onMounted and cleaned up in onUnmounted"
    - "The dark theme (NConfigProvider darkTheme) already configured from Phase 1 continues to apply globally"
  artifacts:
    - path: "src/App.vue"
      provides: "Root component with Naive UI providers wrapping MainLayout"
      min_lines: 70
    - path: "src/components/MainLayout.vue"
      provides: "Dual-panel resizable layout with composable lifecycle"
      min_lines: 60
  key_links:
    - from: "src/App.vue"
      to: "src/components/MainLayout.vue"
      via: "import + conditional render when ffmpegStore.isReady"
      pattern: "import MainLayout from"
    - from: "src/components/MainLayout.vue"
      to: "src/composables/useSeed.ts"
      via: "subscribe() in onMounted, unsubscribe() in onUnmounted"
      pattern: "seedComposable.subscribe"
    - from: "src/components/MainLayout.vue"
      to: "src/components/seed/SeedList.vue"
      via: "import + render inside NLayoutSider"
      pattern: "import SeedList"
---

<objective>
Update App.vue to add Naive UI provider wrappers (NDialogProvider, NMessageProvider, NNotificationProvider) and replace PlaceholderHome with the new MainLayout. Create MainLayout.vue implementing the dual-panel resizable layout with composable subscription lifecycle, matching D-01 (fixed left-right, draggable divider, 50/50 default) and D-02 (right panel: queue top, batch controls bottom).

Purpose: App.vue is the root component already providing NConfigProvider with darkTheme. It must be the ancestor of all provider components so useMessage(), useDialog(), and useNotification() composables function. MainLayout is the app-lifetime container for all Phase 3 panels and composable event subscriptions.

Output: Modified src/App.vue, new src/components/MainLayout.vue
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-vue-frontend/03-CONTEXT.md (D-01, D-02 locked decisions)
@.planning/phases/03-vue-frontend/03-PATTERNS.md (MainLayout.vue and App.vue sections)
@.planning/phases/03-vue-frontend/03-UI-SPEC.md (Layout Contract section, lines 192-229)
@src/App.vue (current state — needs provider wrappers and MainLayout import)
@src/composables/useSeed.ts (created by plan 03-03 — useSeed, subscribe, unsubscribe)
@src/composables/useQueue.ts (created by plan 03-03 — useQueue, subscribe, unsubscribe)
@src/composables/useBatch.ts (created by plan 03-03 — useBatch, subscribe, unsubscribe)

<interfaces>
Current src/App.vue structure (must be preserved and extended):
```html
<script setup lang="ts">
import { computed, ref, onMounted } from 'vue';
import { NConfigProvider, NGlobalStyle, darkTheme, zhCN, enUS, dateZhCN, dateEnUS } from 'naive-ui';
import { useI18n } from 'vue-i18n';
import { useFfmpegStore } from '@/stores/ffmpeg';
import FFmpegStatus from '@/components/FFmpegStatus.vue';
import FFmpegDownload from '@/components/FFmpegDownload.vue';
import PlaceholderHome from '@/components/PlaceholderHome.vue';
// ... locale, view state, event handlers ...
</script>

<template>
  <NConfigProvider :theme="darkTheme" :locale="naiveLocale" :date-locale="naiveDateLocale">
    <NGlobalStyle />
    <FFmpegDownload v-if="showDownload" @done="onDownloadDone" @back="onDownloadBack" />
    <PlaceholderHome v-else-if="ffmpegStore.isReady" />
    <FFmpegStatus v-else @start-download="onStartDownload" />
  </NConfigProvider>
</template>
```

Composable API consumed by MainLayout:
- `useSeed()` → returns `{ subscribe, unsubscribe }`
- `useQueue()` → returns `{ subscribe, unsubscribe }`
- `useBatch()` → returns `{ subscribe, unsubscribe }`

Store API consumed:
- `useFfmpegStore()` → `.isReady` (computed boolean)
- `useBatchStore()` → `.isProcessing` (ref boolean)

NLayout contract from Naive UI (Context7 verified):
- Parent `<n-layout has-sider position="absolute">` REQUIRED when using n-layout-sider
- Child `<n-layout-sider bordered :width="leftWidth" collapse-mode="width">`
- Sibling `<n-layout-content :native-scrollbar="false">`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.vue with Naive UI providers and MainLayout import</name>
  <files>src/App.vue</files>
  <read_first>
  - src/App.vue — current state (must read before modifying to understand exact existing content)
  - src/components/MainLayout.vue — WILL be created by Task 2 (import path reference)
  </read_first>
  <action>
  Modify `src/App.vue` to add Naive UI provider components and import MainLayout. The key changes are:

  1. Add three new imports from 'naive-ui': `NDialogProvider`, `NMessageProvider`, `NNotificationProvider`
  2. Add import for MainLayout component (replace PlaceholderHome usage)
  3. Wrap the content tree (FFmpegDownload, PlaceholderHome, FFmpegStatus) inside the provider hierarchy

  Here is the exact updated file content. Preserve ALL existing logic (locale computed, showDownload ref, event handlers, onMounted):

  ```html
  <script setup lang="ts">
  import { computed, ref, onMounted } from 'vue';
  import {
    NConfigProvider,
    NGlobalStyle,
    NDialogProvider,
    NMessageProvider,
    NNotificationProvider,
    darkTheme,
    zhCN as naiveZhCN,
    enUS as naiveEnUS,
    dateZhCN,
    dateEnUS,
  } from 'naive-ui';
  import { useI18n } from 'vue-i18n';
  import { useFfmpegStore } from '@/stores/ffmpeg';
  import FFmpegStatus from '@/components/FFmpegStatus.vue';
  import FFmpegDownload from '@/components/FFmpegDownload.vue';
  import MainLayout from '@/components/MainLayout.vue';

  const { locale } = useI18n();
  const ffmpegStore = useFfmpegStore();

  // Naive UI locale follows vue-i18n locale
  const naiveLocale = computed(() => {
    if (locale.value === 'zh-CN') return naiveZhCN;
    return naiveEnUS;
  });
  const naiveDateLocale = computed(() => {
    if (locale.value === 'zh-CN') return dateZhCN;
    return dateEnUS;
  });

  // Current view state based on FFmpeg status
  const showDownload = ref(false);

  function onStartDownload() {
    showDownload.value = true;
  }

  function onDownloadDone() {
    showDownload.value = false;
  }

  function onDownloadBack() {
    showDownload.value = false;
  }

  onMounted(() => {
    // Locale can be detected from navigator.language if desired;
    // for now, default to zh-CN as set in i18n.ts
  });
  </script>

  <template>
    <NConfigProvider :theme="darkTheme" :locale="naiveLocale" :date-locale="naiveDateLocale">
      <NGlobalStyle />
      <n-dialog-provider>
        <n-message-provider :max="5" placement="top-right">
          <n-notification-provider placement="top-right" :max="5">
            <!-- Download page (full-screen overlay when download is active) -->
            <FFmpegDownload v-if="showDownload" @done="onDownloadDone" @back="onDownloadBack" />

            <!-- Main application layout (shown after FFmpeg is verified) -->
            <MainLayout v-else-if="ffmpegStore.isReady" />

            <!-- Status page (default: detecting, missing, found transition, error) -->
            <FFmpegStatus v-else @start-download="onStartDownload" />
          </n-notification-provider>
        </n-message-provider>
      </n-dialog-provider>
    </NConfigProvider>
  </template>
  ```

  Key changes from original:
  - Added imports: `NDialogProvider`, `NMessageProvider`, `NNotificationProvider` from 'naive-ui'
  - Replaced `import PlaceholderHome from '@/components/PlaceholderHome.vue'` with `import MainLayout from '@/components/MainLayout.vue'`
  - Template: added `<n-dialog-provider>`, `<n-message-provider :max="5" placement="top-right">`, `<n-notification-provider placement="top-right" :max="5">` wrapper hierarchy inside NConfigProvider but around the v-if chain
  - `<PlaceholderHome v-else-if="ffmpegStore.isReady" />` replaced with `<MainLayout v-else-if="ffmpegStore.isReady" />`
  - Provider nesting order (dialog → message → notification) is order-independent per Naive UI docs, but this order matches the established pattern
  - NGlobalStyle stays directly inside NConfigProvider (NOT inside providers — GlobalStyle must be at config-provider level)
  - All existing script logic preserved unchanged: locale computed, showDownload ref, event handlers, onMounted
  </action>
  <verify>
    <automated>grep -c "NDialogProvider" src/App.vue && grep -c "NMessageProvider" src/App.vue && grep -c "NNotificationProvider" src/App.vue && grep -c "MainLayout" src/App.vue && grep -c "PlaceholderHome" src/App.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/App.vue` imports `NDialogProvider`, `NMessageProvider`, `NNotificationProvider` from 'naive-ui'
  - `src/App.vue` imports `MainLayout` from `@/components/MainLayout.vue`
  - `src/App.vue` does NOT import `PlaceholderHome` (removed)
  - Template contains `<n-dialog-provider>` wrapping `<n-message-provider :max="5" placement="top-right">` wrapping `<n-notification-provider placement="top-right" :max="5">`
  - `<MainLayout v-else-if="ffmpegStore.isReady" />` renders when FFmpeg is verified
  - `<FFmpegStatus v-else @start-download="onStartDownload" />` still renders when FFmpeg is not ready
  - `<FFmpegDownload v-if="showDownload" ... />` still renders as full-screen overlay when download is active
  - `<NGlobalStyle />` remains directly inside NConfigProvider (NOT nested inside dialog/message/notification providers)
  - All existing script logic (locale, showDownload, event handlers, onMounted) is unchanged
  - `<NConfigProvider :theme="darkTheme">` still present (D-02, dark theme unchanged)
  </acceptance_criteria>
  <done>App.vue updated with Naive UI provider wrappers and MainLayout import</done>
</task>

<task type="auto">
  <name>Task 2: Create MainLayout.vue (dual-panel resizable layout + composable subscriptions)</name>
  <files>src/components/MainLayout.vue</files>
  <read_first>
  - src/App.vue — updated by Task 1, see how MainLayout is imported and where it renders
  - src/components/FFmpegStatus.vue — existing component pattern (imports, onMounted subscriptions, template structure with NLayout)
  - src/composables/useSeed.ts — useSeed composable (subscribe, unsubscribe)
  - src/composables/useQueue.ts — useQueue composable (subscribe, unsubscribe)
  - src/composables/useBatch.ts — useBatch composable (subscribe, unsubscribe)
  - src/stores/batch.ts — useBatchStore (isProcessing ref for BatchBanner visibility)
  - .planning/phases/03-vue-frontend/03-UI-SPEC.md — Layout Contract section for exact panel specs and ASCII diagram
  </read_first>
  <action>
  Create `src/components/MainLayout.vue` implementing the dual-panel resizable layout per D-01 and D-02.

  **Layout structure (per D-01, D-02):**
  - Full-viewport NLayout with `position="absolute"` and `has-sider`
  - Left: NLayoutSider (seed panel, min 280px, default 50% viewport, max 70%)
  - Right: NLayoutContent with two sections:
    - Upper: queue area (ImportZone + BatchBanner conditional + QueueList) — scrollable
    - Lower: BatchControls in NLayoutFooter — sticky at bottom

  **Resize handle (D-01):**
  - A 4px-wide div between sider and content
  - pointerdown starts drag, pointermove adjusts leftWidth (clamped 250px–70% viewport), pointerup ends drag
  - user-select: none and cursor: col-resize during drag

  **Composable lifecycle (Pitfall 2 avoidance):**
  - Subscribe all three composables in onMounted
  - Unsubscribe all three in onUnmounted
  - MainLayout is the app-lifetime component — listeners registered once, never duplicated

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { ref, onMounted, onUnmounted } from 'vue';
  import { NLayout, NLayoutSider, NLayoutContent, NLayoutFooter } from 'naive-ui';
  import { useBatchStore } from '@/stores/batch';
  import { useSeed } from '@/composables/useSeed';
  import { useQueue } from '@/composables/useQueue';
  import { useBatch } from '@/composables/useBatch';
  import SeedList from '@/components/seed/SeedList.vue';
  import ImportZone from '@/components/queue/ImportZone.vue';
  import QueueList from '@/components/queue/QueueList.vue';
  import BatchControls from '@/components/batch/BatchControls.vue';
  import BatchBanner from '@/components/batch/BatchBanner.vue';

  const batchStore = useBatchStore();
  const seedComposable = useSeed();
  const queueComposable = useQueue();
  const batchComposable = useBatch();

  // Panel sizing (D-01: default 50/50, draggable divider)
  const leftWidth = ref(Math.floor(window.innerWidth / 2));
  const isResizing = ref(false);

  function onResizePointerdown(e: PointerEvent) {
    e.preventDefault();
    isResizing.value = true;
    document.body.style.userSelect = 'none';
    document.body.style.cursor = 'col-resize';
  }

  function onResizePointermove(e: PointerEvent) {
    if (!isResizing.value) return;
    const w = e.clientX;
    leftWidth.value = Math.max(250, Math.min(w, Math.floor(window.innerWidth * 0.7)));
  }

  function onResizePointerup() {
    isResizing.value = false;
    document.body.style.userSelect = '';
    document.body.style.cursor = '';
  }

  // Attach global move/up listeners when resizing starts
  onMounted(() => {
    window.addEventListener('pointermove', onResizePointermove);
    window.addEventListener('pointerup', onResizePointerup);
  });

  onUnmounted(() => {
    window.removeEventListener('pointermove', onResizePointermove);
    window.removeEventListener('pointerup', onResizePointerup);
  });

  // Composable subscriptions (app-lifetime — registered once, never duplicated)
  onMounted(async () => {
    await seedComposable.subscribe();
    await queueComposable.subscribe();
    await batchComposable.subscribe();
  });

  onUnmounted(() => {
    seedComposable.unsubscribe();
    queueComposable.unsubscribe();
    batchComposable.unsubscribe();
  });
  </script>

  <template>
    <n-layout style="height: 100vh; height: 100dvh;" position="absolute">
      <n-layout has-sider position="absolute" style="top: 0; bottom: 0;">
        <!-- Left Panel: Seed Management -->
        <n-layout-sider bordered :width="leftWidth" collapse-mode="width">
          <SeedList />
        </n-layout-sider>

        <!-- Resize Handle (D-01: draggable divider) -->
        <div
          class="resize-handle"
          :class="{ 'resize-handle--active': isResizing }"
          @pointerdown="onResizePointerdown"
        />

        <!-- Right Panel: Queue + Batch Controls -->
        <n-layout-content :native-scrollbar="false">
          <div class="right-panel">
            <!-- Queue Area (upper section per D-02) -->
            <div class="queue-area">
              <ImportZone />
              <BatchBanner v-if="batchStore.isProcessing" />
              <QueueList />
            </div>

            <!-- Batch Controls (lower section per D-02, sticky footer) -->
            <n-layout-footer class="batch-footer">
              <BatchControls />
            </n-layout-footer>
          </div>
        </n-layout-content>
      </n-layout>
    </n-layout>
  </template>

  <style scoped>
  .resize-handle {
    width: 4px;
    cursor: col-resize;
    background-color: transparent;
    transition: background-color 0.2s;
    flex-shrink: 0;
    z-index: 10;
  }
  .resize-handle:hover,
  .resize-handle--active {
    background-color: #2080f0;
  }

  .right-panel {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .queue-area {
    flex: 1;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
  }

  .batch-footer {
    flex-shrink: 0;
    border-top: 1px solid var(--n-border-color);
    background-color: var(--n-color);
  }
  </style>
  ```

  Key rules applied:
  - `has-sider` prop on parent n-layout (Pitfall 1 avoidance — REQUIRED for Naive UI v2.3.0+)
  - `collapse-mode="width"` on sider prevents collapse animation issues with drag resize
  - Resize handle uses `window.addEventListener` for pointermove/pointerup (not element-level) to track drag outside the handle
  - `user-select: none` on body during drag prevents text selection
  - `100dvh` fallback with `100vh` for mobile-safe viewport height
  - BatchBanner conditionally rendered inside queue-area (above QueueList) — only when batch is processing
  - BatchControls in NLayoutFooter is sticky at bottom of right panel
  - Two onMounted blocks: one for resize event listeners, one for composable subscriptions (both called on mount, order irrelevant)
  - Two onUnmounted blocks: cleanup for resize listeners and composable unsubscribes
  </action>
  <verify>
    <automated>grep -c "NLayout" src/components/MainLayout.vue && grep -c "NLayoutSider" src/components/MainLayout.vue && grep -c "has-sider" src/components/MainLayout.vue && grep -c "NLayoutFooter" src/components/MainLayout.vue && grep -c "SeedList" src/components/MainLayout.vue && grep -c "ImportZone" src/components/MainLayout.vue && grep -c "QueueList" src/components/MainLayout.vue && grep -c "BatchControls" src/components/MainLayout.vue && grep -c "BatchBanner" src/components/MainLayout.vue && grep -c "seedComposable.subscribe" src/components/MainLayout.vue && grep -c "seedComposable.unsubscribe" src/components/MainLayout.vue && grep -c "resize-handle" src/components/MainLayout.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/MainLayout.vue` exists with `<script setup lang="ts">`
  - Imports NLayout, NLayoutSider, NLayoutContent, NLayoutFooter from 'naive-ui'
  - Imports SeedList, ImportZone, QueueList, BatchControls, BatchBanner components
  - Uses `useSeed()`, `useQueue()`, `useBatch()` composables
  - Calls `seedComposable.subscribe()`, `queueComposable.subscribe()`, `batchComposable.subscribe()` in onMounted
  - Calls `seedComposable.unsubscribe()`, `queueComposable.unsubscribe()`, `batchComposable.unsubscribe()` in onUnmounted
  - Template has `<n-layout has-sider position="absolute">` wrapping `<n-layout-sider>` and `<n-layout-content>`
  - NLayoutSider has `:width="leftWidth"` and `collapse-mode="width"`
  - Resize handle div with class `resize-handle` between sider and content, using @pointerdown/pointermove/pointerup
  - Resize logic: `leftWidth` initialized to `Math.floor(window.innerWidth / 2)`, clamped to 250–70% viewport
  - `isResizing` ref sets `document.body.style.userSelect = 'none'` and `cursor = 'col-resize'` during drag
  - Queue area contains `<ImportZone />`, `<BatchBanner v-if="batchStore.isProcessing" />`, `<QueueList />`
  - NLayoutFooter contains `<BatchControls />` as sticky bottom section
  - `batchStore.isProcessing` controls BatchBanner visibility
  - Scoped style: `.resize-handle` 4px width, `.resize-handle--active` with `#2080f0` background
  - Uses `<style scoped>` (NOT global styles)
  </acceptance_criteria>
  <done>MainLayout created with dual-panel resizable layout and composable lifecycle</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview DOM → Tauri IPC | Resize events are pure DOM — no data crosses trust boundary. Subscriptions cross into Tauri event system (trusted backend source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Denial of Service | Resize handle drag | accept | DOM pointer events are browser-native. User can only resize within clamped bounds. No external input. |
| T-03-10 | Elevation of Privilege | Composable subscriptions | accept | Composables only invoke documented IPC commands. Rust backend validates all inputs. No privilege escalation path. |
</threat_model>

<verification>
```bash
# Verify MainLayout has the required structure
grep -c "has-sider" src/components/MainLayout.vue && \
grep -c "NLayoutFooter" src/components/MainLayout.vue && \
grep -c "subscribe()" src/components/MainLayout.vue && \
grep -c "unsubscribe()" src/components/MainLayout.vue && \
echo "MainLayout structure verified"

# Verify App.vue has provider wrappers
grep -c "n-dialog-provider" src/App.vue && \
grep -c "n-message-provider" src/App.vue && \
grep -c "MainLayout" src/App.vue && \
echo "App.vue providers verified"
```
</verification>

<success_criteria>
- [ ] App.vue wraps MainLayout in NDialogProvider, NMessageProvider (max:5, top-right), NNotificationProvider (max:5, top-right)
- [ ] App.vue replaces PlaceholderHome with MainLayout import and v-else-if rendering
- [ ] App.vue preserves all existing FFmpeg status/download UI logic unchanged
- [ ] MainLayout.vue implements NLayout with has-sider, NLayoutSider (left), resize handle, NLayoutContent (right)
- [ ] MainLayout initializes all three composable subscriptions on mount and cleans up on unmount
- [ ] Resize handle supports drag between 250px and 70% viewport with visual feedback
- [ ] Right panel has ImportZone + BatchBanner + QueueList (scrollable upper) and BatchControls (sticky footer lower)
- [ ] Dark theme applies globally via existing NConfigProvider darkTheme (no changes needed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-vue-frontend/03-04-SUMMARY.md`
</output>
