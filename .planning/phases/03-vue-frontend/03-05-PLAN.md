---
phase: 03-vue-frontend
plan: 05
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/components/seed/SeedCard.vue
  - src/components/seed/SeedList.vue
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "SeedList renders the left panel header (title + generate button) and either empty-state CTA or scrollable seed card list"
    - "SeedCard displays seed alias (bold), operation type tags (up to 3 + overflow count), and creation timestamp"
    - "Action buttons (rename/copy/delete) appear only on card hover per D-06"
    - "Delete seed triggers NPopconfirm before calling deleteSeed per D-09"
    - "Empty state directly calls generateSeed when 'Generate First Seed' is clicked per D-08"
    - "Selecting a card updates the selectedSeedId in the store and visually highlights with accent border"
  artifacts:
    - path: "src/components/seed/SeedCard.vue"
      provides: "Individual seed card with hover-revealed actions and selection state"
      min_lines: 80
    - path: "src/components/seed/SeedList.vue"
      provides: "Seed panel with header, empty state, and scrollable card list"
      min_lines: 50
  key_links:
    - from: "src/components/seed/SeedCard.vue"
      to: "src/composables/useSeed.ts"
      via: "copySeed() and deleteSeed() calls on button click"
      pattern: "copySeed"
    - from: "src/components/seed/SeedList.vue"
      to: "src/composables/useSeed.ts"
      via: "generateSeed() call from header button and empty state button"
      pattern: "generateSeed"
    - from: "src/components/seed/SeedCard.vue"
      to: "src/stores/seed.ts"
      via: "selectedSeedId for visual selection highlight"
      pattern: "store.selectedSeedId"
---

<objective>
Create SeedList.vue (left panel container with header, empty state, and scrollable seed card list) and SeedCard.vue (individual seed card with alias, operation tags, creation time, and hover-revealed action buttons). Per D-05 (compact card layout), D-06 (hover-only actions), D-07 (guided empty state), D-08 (empty state triggers action), and D-09 (tiered confirmation for delete).

Purpose: The seed management UI is the left half of the dual-panel layout. Users can see all seeds as compact cards, generate new seeds with one click, and manage existing seeds (rename, copy, delete) with appropriate confirmation levels.

Output: 2 new component files in `src/components/seed/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-vue-frontend/03-CONTEXT.md (D-05, D-06, D-07, D-08, D-09)
@.planning/phases/03-vue-frontend/03-PATTERNS.md (SeedList.vue and SeedCard.vue sections)
@.planning/phases/03-vue-frontend/03-UI-SPEC.md (Seed Card Contract lines 231-265, Empty States, Copywriting)
@src/components/FFmpegStatus.vue (state-driven conditional rendering pattern)
@src/composables/useSeed.ts (useSeed composable: generateSeed, renameSeed, deleteSeed, copySeed)
@src/stores/seed.ts (useSeedStore: seeds, selectedSeedId, selectedSeed, seedCount, selectSeed)

<interfaces>
Composable API (useSeed):
- `generateSeed(): Promise<Seed | null>`
- `renameSeed(seedId: string, newAlias: string): Promise<boolean>`
- `deleteSeed(seedId: string): Promise<boolean>`
- `copySeed(seedId: string): Promise<Seed | null>`

Store API (useSeedStore):
- `seeds: Ref<Seed[]>` — reactive seed list
- `selectedSeedId: Ref<string | null>` — currently selected seed ID
- `selectedSeed: ComputedRef<Seed | null>` — derived from selectedSeedId
- `seedCount: ComputedRef<number>` — length of seeds array
- `selectSeed(id: string | null): void` — sets selectedSeedId

Naive UI components needed (per UI-SPEC component inventory):
- NButton, NIcon, NText, NCard, NTag, NInput, NPopconfirm, useMessage

Icons (lucide-vue-next):
- Sparkles (seed generation), Plus (generate button), Pencil (rename), Copy (copy), Trash2 (delete), Zap (selected indicator)

i18n keys:
- seed.title, seed.empty, seed.emptyCta, seed.generate, seed.generated
- seed.rename, seed.renamed, seed.copy, seed.copied
- seed.delete, seed.deleteConfirm, seed.deleted
- seed.aliasPlaceholder, seed.aliasEmpty

UI-SPEC Seed Card Contract:
- Default: NCard with muted border, alias bold 16px 600 weight, NTag operation tags (info type, bordered=false), caption timestamp 12px depth="3"
- Hover: border brightens, action buttons appear (opacity 0→1, translateX -4px→0)
- Selected: 2px solid #2080f0 border, elevated background
- Max 3 operation tags visible; if >3, show "+N more" tag
- Rename flow: Pencil click → NInput replaces alias text inline, Enter confirms/Esc cancels
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SeedCard.vue (seed card with hover actions, selection, rename flow)</name>
  <files>src/components/seed/SeedCard.vue</files>
  <read_first>
  - src/components/FFmpegStatus.vue — Naive UI component import pattern, NSpace vertical layout, state-driven rendering
  - src/components/FFmpegDownload.vue — NCard usage, NButton with icons, conditional display
  - src/types/seed.ts — Seed, Operation, OperationType type definitions
  - src/stores/seed.ts — useSeedStore API
  - src/composables/useSeed.ts — useSeed composable API
  </read_first>
  <action>
  Create `src/components/seed/SeedCard.vue` implementing the seed card contract per D-05 (compact card layout), D-06 (hover-only action buttons), D-09 (NPopconfirm for delete), and the UI-SPEC Seed Card states.

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { ref } from 'vue';
  import { NCard, NButton, NIcon, NText, NTag, NPopconfirm, NInput, NSpace } from 'naive-ui';
  import { Pencil, Copy, Trash2, Zap } from 'lucide-vue-next';
  import { useMessage } from 'naive-ui';
  import type { Seed } from '@/types/seed';
  import { useSeedStore } from '@/stores/seed';
  import { useSeed } from '@/composables/useSeed';
  import { useI18n } from 'vue-i18n';

  const props = defineProps<{
    seed: Seed;
  }>();

  const store = useSeedStore();
  const { renameSeed, deleteSeed, copySeed } = useSeed();
  const message = useMessage();
  const { t } = useI18n();

  const isHovered = ref(false);
  const isRenaming = ref(false);
  const renameValue = ref('');

  const isSelected = () => store.selectedSeedId === props.seed.id;

  /** Display up to 3 operation type tags; show "+N more" if >3. */
  function visibleOps() {
    return props.seed.operations.slice(0, 3).map(o => o.opType);
  }
  function overflowCount() {
    return Math.max(0, props.seed.operations.length - 3);
  }

  function onSelect() {
    // Toggle selection: click same card deselects, click different selects
    store.selectSeed(isSelected() ? null : props.seed.id);
  }

  function startRename() {
    renameValue.value = props.seed.alias;
    isRenaming.value = true;
  }

  async function confirmRename() {
    const trimmed = renameValue.value.trim();
    if (!trimmed) {
      message.warning(t('seed.aliasEmpty'));
      return;
    }
    const ok = await renameSeed(props.seed.id, trimmed);
    if (ok) {
      message.success(t('seed.renamed', { alias: trimmed }));
    } else {
      message.error(t('notification.operationFailed', { error: 'Rename failed' }));
    }
    isRenaming.value = false;
  }

  function cancelRename() {
    isRenaming.value = false;
  }

  async function onCopy() {
    const newSeed = await copySeed(props.seed.id);
    if (newSeed) {
      message.success(t('seed.copied', { alias: newSeed.alias }));
    } else {
      message.error(t('notification.operationFailed', { error: 'Copy failed' }));
    }
  }

  async function onDelete() {
    const ok = await deleteSeed(props.seed.id);
    if (ok) {
      message.success(t('seed.deleted'));
    } else {
      message.error(t('notification.operationFailed', { error: 'Delete failed' }));
    }
  }
  </script>

  <template>
    <NCard
      :bordered="true"
      :class="[
        'cursor-pointer transition-all duration-200',
        isSelected() ? 'border-[#2080f0]! border-2!' : 'border-transparent',
      ]"
      @click="onSelect"
      @mouseenter="isHovered = true"
      @mouseleave="isHovered = false"
    >
      <div class="flex items-center justify-between gap-2">
        <div class="flex-1 min-w-0">
          <!-- Selected indicator + Alias -->
          <div class="flex items-center gap-1.5">
            <NIcon v-if="isSelected()" :size="14" color="#2080f0">
              <Zap />
            </NIcon>
            <!-- Inline rename: NInput replaces alias text when renaming -->
            <NInput
              v-if="isRenaming"
              :value="renameValue"
              :placeholder="t('seed.aliasPlaceholder')"
              size="small"
              class="flex-1"
              @update:value="(v: string) => renameValue = v"
              @keyup.enter="confirmRename"
              @keyup.esc="cancelRename"
              @blur="cancelRename"
            />
            <NText v-else strong class="truncate">
              {{ props.seed.alias }}
            </NText>
          </div>

          <!-- Operation type tags -->
          <div class="flex items-center gap-1 mt-1.5 flex-wrap">
            <NTag
              v-for="op in visibleOps()"
              :key="op"
              type="info"
              :bordered="false"
              size="small"
            >
              {{ op }}
            </NTag>
            <NTag v-if="overflowCount() > 0" :bordered="false" size="small">
              +{{ overflowCount() }} more
            </NTag>
          </div>

          <!-- Creation timestamp -->
          <NText depth="3" class="text-xs block mt-1">
            {{ props.seed.createdAt }}
          </NText>
        </div>

        <!-- Action buttons (hover reveal per D-06) -->
        <div
          v-show="isHovered"
          class="flex items-center gap-1 shrink-0"
          :class="['transition-opacity duration-200', isHovered ? 'opacity-100' : 'opacity-0']"
          @click.stop
        >
          <NButton size="tiny" quaternary @click="startRename">
            <template #icon><NIcon :size="16"><Pencil /></NIcon></template>
          </NButton>
          <NButton size="tiny" quaternary @click="onCopy">
            <template #icon><NIcon :size="16"><Copy /></NIcon></template>
          </NButton>
          <NPopconfirm @positive-click="onDelete">
            <template #trigger>
              <NButton size="tiny" quaternary type="error">
                <template #icon><NIcon :size="16"><Trash2 /></NIcon></template>
              </NButton>
            </template>
            {{ t('seed.deleteConfirm', { alias: props.seed.alias }) }}
          </NPopconfirm>
        </div>
      </div>
    </NCard>
  </template>
  ```

  Key design decisions:
  - Selection toggle: clicking selected card deselects (store.selectSeed(null)), clicking unselected card selects (store.selectSeed(props.seed.id))
  - Rename uses inline NInput that appears in place of alias text, Enter to confirm, Esc/blur to cancel
  - NPopconfirm on delete per D-09 (destructive action gets confirmation dialog)
  - Copy and rename are silent execution per D-09 (no confirmation, just useMessage success/error toast)
  - `@click.stop` on action button container prevents card selection when clicking action buttons
  - Operation tags: max 3 visible, "+N more" tag for overflow
  - Selected state: 2px solid #2080f0 border with Zap icon indicator
  - `border-[#2080f0]! border-2!` uses UnoCSS important modifier to override NCard default border
  </action>
  <verify>
    <automated>grep -c "NCard" src/components/seed/SeedCard.vue && grep -c "NPopconfirm" src/components/seed/SeedCard.vue && grep -c "NInput" src/components/seed/SeedCard.vue && grep -c "isRenaming" src/components/seed/SeedCard.vue && grep -c "isHovered" src/components/seed/SeedCard.vue && grep -c "copySeed" src/components/seed/SeedCard.vue && grep -c "deleteSeed" src/components/seed/SeedCard.vue && grep -c "renameSeed" src/components/seed/SeedCard.vue && grep -c "useMessage" src/components/seed/SeedCard.vue && grep -c "selectSeed" src/components/seed/SeedCard.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/seed/SeedCard.vue` exists with `<script setup lang="ts">`
  - Props: `seed: Seed` (single seed object)
  - Displays alias as bold NText, operation type NTag array (max 3 + "+N more"), creation timestamp as depth="3" caption
  - Hover state: `isHovered` ref bound to @mouseenter/@mouseleave; action buttons wrapped in `v-show="isHovered"` with CSS opacity transition
  - Action buttons: Pencil (rename), Copy (copy), Trash2 (delete)
  - Delete uses NPopconfirm with i18n confirmation message `seed.deleteConfirm`
  - Copy calls `copySeed(props.seed.id)` then shows `useMessage().success(t('seed.copied', ...))`
  - Rename: clicking Pencil sets `isRenaming = true`, NInput replaces alias with pre-filled value; Enter calls `confirmRename()`, Esc/blur calls `cancelRename()`
  - `confirmRename` validates non-empty (shows warning if empty), calls `renameSeed`, shows success/error toast
  - Selection: clicking card body calls `store.selectSeed(...)`; toggle behavior (click selected → deselect)
  - Selected visual: `border-[#2080f0]! border-2!` class when `isSelected()` true, plus Zap icon
  - `@click.stop` on action button container prevents card selection click-through
  - All user-visible text uses `t()` i18n function
  - `<style scoped>` if custom styles needed (UnoCSS utility classes preferred)
  </acceptance_criteria>
  <done>SeedCard created with hover actions, inline rename, NPopconfirm delete, and selection state</done>
</task>

<task type="auto">
  <name>Task 2: Create SeedList.vue (seed panel with header, empty state, card list)</name>
  <files>src/components/seed/SeedList.vue</files>
  <read_first>
  - src/components/FFmpegStatus.vue — state-driven v-if rendering pattern (empty/missing/found states)
  - src/components/seed/SeedCard.vue — created by Task 1, imported and rendered in v-for
  - src/stores/seed.ts — useSeedStore (seeds, seedCount)
  - src/composables/useSeed.ts — useSeed composable (generateSeed)
  </read_first>
  <action>
  Create `src/components/seed/SeedList.vue` implementing the left panel seed management UI per D-07 (guided empty state) and D-08 (empty state CTA triggers action).

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { NButton, NIcon, NText, NCard, NScrollbar } from 'naive-ui';
  import { Sparkles, Plus } from 'lucide-vue-next';
  import { useMessage } from 'naive-ui';
  import { useSeedStore } from '@/stores/seed';
  import { useSeed } from '@/composables/useSeed';
  import { useI18n } from 'vue-i18n';
  import SeedCard from './SeedCard.vue';

  const store = useSeedStore();
  const { generateSeed } = useSeed();
  const message = useMessage();
  const { t } = useI18n();

  async function onGenerateSeed() {
    const seed = await generateSeed();
    if (seed) {
      message.success(t('seed.generated', { alias: seed.alias }));
    } else {
      message.error(t('notification.operationFailed', { error: 'Generate failed' }));
    }
  }
  </script>

  <template>
    <div class="flex flex-col h-full">
      <!-- Header: title + generate button -->
      <div class="flex items-center justify-between px-4 py-3 shrink-0 border-b border-[#2a2a36]">
        <NText strong class="text-base">
          {{ t('seed.title') }} ({{ store.seedCount }})
        </NText>
        <NButton type="primary" size="small" @click="onGenerateSeed">
          <template #icon>
            <NIcon :size="16"><Plus /></NIcon>
          </template>
          {{ t('seed.generate') }}
        </NButton>
      </div>

      <!-- Empty State (D-07, D-08) -->
      <div v-if="store.seedCount === 0" class="flex-1 flex items-center justify-center p-6">
        <NCard :bordered="false" class="text-center">
          <NSpace vertical align="center" :size="16">
            <NIcon :size="48" color="#2080f0">
              <Sparkles />
            </NIcon>
            <NText depth="2">{{ t('seed.empty') }}</NText>
            <NButton type="primary" size="large" @click="onGenerateSeed">
              <template #icon>
                <NIcon :size="18"><Sparkles /></NIcon>
              </template>
              {{ t('seed.emptyCta') }}
            </NButton>
          </NSpace>
        </NCard>
      </div>

      <!-- Seed List (scrollable) -->
      <NScrollbar v-else class="flex-1">
        <div class="px-4 py-3 space-y-2">
          <SeedCard
            v-for="s in store.seeds"
            :key="s.id"
            :seed="s"
          />
        </div>
      </NScrollbar>
    </div>
  </template>
  ```

  Key design decisions:
  - Header is fixed at top (shrink-0) with title showing count and primary "Generate Seed" button
  - Empty state: Sparkles icon (48px, accent color), descriptive text, large primary CTA button
  - Empty state CTA directly calls `generateSeed()` per D-08
  - Populated state: NScrollbar wrapping v-for of SeedCard components with 8px gap (space-y-2)
  - Both header button and empty state button call the same `onGenerateSeed()` handler
  - Success/error feedback via useMessage() per D-10
  - `border-b border-[#2a2a36]` on header for visual separation in dark theme
  </action>
  <verify>
    <automated>grep -c "SeedCard" src/components/seed/SeedList.vue && grep -c "seedCount" src/components/seed/SeedList.vue && grep -c "Sparkles" src/components/seed/SeedList.vue && grep -c "generateSeed" src/components/seed/SeedList.vue && grep -c "useMessage" src/components/seed/SeedList.vue && grep -c "NScrollbar" src/components/seed/SeedList.vue && grep -c "emptyCta" src/components/seed/SeedList.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/seed/SeedList.vue` exists with `<script setup lang="ts">`
  - Imports and renders `SeedCard` component in `v-for="s in store.seeds" :key="s.id" :seed="s"`
  - Header shows `t('seed.title')` with `({{ store.seedCount }})` and primary NButton "Generate Seed" with Plus icon
  - Empty state (v-if="store.seedCount === 0"): Sparkles icon 48px, descriptive text, large primary CTA button with t('seed.emptyCta')
  - Empty state button calls `onGenerateSeed()` which invokes `generateSeed()` and shows success/error toast
  - Populated state (v-else): NScrollbar wrapping seed card list with `space-y-2` gap
  - `onGenerateSeed` calls `useMessage().success(t('seed.generated', ...))` on success
  - `onGenerateSeed` calls `useMessage().error(t('notification.operationFailed', ...))` on failure
  - Full-height flex column layout (`flex flex-col h-full`)
  - Header is shrink-0 (non-scrollable), card list is flex-1 (fills remaining space)
  - All user-visible text uses `t()` i18n function
  </acceptance_criteria>
  <done>SeedList created with header, guided empty state, and scrollable seed card list</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → IPC | Rename alias text, delete confirmation — passed to Rust IPC commands which validate on backend |
| Vue template → DOM | Seed alias is rendered via `{{ }}` which auto-escapes HTML (XSS prevention built into Vue) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | SeedCard rename NInput value | mitigate | Rust `rename_seed` validates alias is non-empty. Frontend also checks `trim()` before submit. |
| T-03-12 | Spoofing | SeedCard delete NPopconfirm | accept | Confirmation dialog is UI-only (not a security boundary). Actual deletion happens in Rust backend with seed ID validation. |
</threat_model>

<verification>
```bash
# Verify both seed components exist with required elements
grep -c "NCard" src/components/seed/SeedCard.vue && \
grep -c "NPopconfirm" src/components/seed/SeedCard.vue && \
grep -c "NInput" src/components/seed/SeedCard.vue && \
grep -c "NScrollbar" src/components/seed/SeedList.vue && \
grep -c "SeedCard" src/components/seed/SeedList.vue && \
echo "Seed components verified"
```
</verification>

<success_criteria>
- [ ] SeedCard.vue renders alias (bold), operation tags (max 3 + overflow), and creation timestamp
- [ ] SeedCard.vue action buttons appear only on hover (v-show="isHovered" with opacity transition)
- [ ] SeedCard.vue rename uses inline NInput; Enter confirms, Esc/blur cancels
- [ ] SeedCard.vue delete uses NPopconfirm with i18n confirmation text
- [ ] SeedCard.vue copy shows success toast on completion
- [ ] SeedCard.vue selection toggles on card click with accent border and Zap icon
- [ ] SeedList.vue renders header (title + count + generate button)
- [ ] SeedList.vue shows guided empty state when no seeds (icon + text + CTA button)
- [ ] SeedList.vue renders scrollable SeedCard list when seeds exist
- [ ] Both components use useMessage() for operation feedback (D-10)
- [ ] All text uses vue-i18n t() function
</success_criteria>

<output>
After completion, create `.planning/phases/03-vue-frontend/03-05-SUMMARY.md`
</output>
