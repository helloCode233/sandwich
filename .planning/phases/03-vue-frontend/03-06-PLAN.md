---
phase: 03-vue-frontend
plan: 06
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/components/queue/ImportZone.vue
  - src/components/queue/QueueList.vue
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "ImportZone renders a prominent drag-and-drop hot zone that accepts video files and a 'Add Files' button that opens native file dialog per D-03"
    - "Drag-over provides visual feedback (accent border + background highlight + text change to 'Drop to import') per UI-SPEC"
    - "QueueList renders a data table of queued video entries showing filename, metadata, status, and per-item remove button"
    - "QueueList shows guided empty state when queue is empty (icon + instructional text + 'Add Videos' CTA) per D-07"
    - "Clear All button triggers NModal confirmation before calling clearQueue per D-09"
    - "Each imported file shows success/error toast via useMessage() per D-10"
    - "Queue list auto-refreshes after video-imported event per D-04"
  artifacts:
    - path: "src/components/queue/ImportZone.vue"
      provides: "HTML5 drag-drop zone + file dialog button for video import"
      min_lines: 70
    - path: "src/components/queue/QueueList.vue"
      provides: "Video queue table with metadata, status, and item removal"
      min_lines: 80
  key_links:
    - from: "src/components/queue/ImportZone.vue"
      to: "src/composables/useQueue.ts"
      via: "importVideo(filepath) call on drop or file dialog selection"
      pattern: "importVideo"
    - from: "src/components/queue/QueueList.vue"
      to: "src/composables/useQueue.ts"
      via: "removeFromQueue(index) and clearQueue() calls"
      pattern: "removeFromQueue"
    - from: "src/components/queue/ImportZone.vue"
      to: "@tauri-apps/plugin-dialog"
      via: "open() for native file picker with video extension filter"
      pattern: "import { open }"
---

<objective>
Create ImportZone.vue (HTML5 drag-and-drop video import zone with native file dialog fallback per D-03) and QueueList.vue (video queue display with metadata table, per-item remove, clear-all with NModal confirmation per D-09, and guided empty state per D-07/D-08).

Purpose: The video import and queue management UI is the upper section of the right panel. Users drag videos into the hot zone or use the file dialog to import. QueueList shows all imported videos with metadata, status indicators, and queue management actions.

Output: 2 new component files in `src/components/queue/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-vue-frontend/03-CONTEXT.md (D-03, D-04, D-07, D-08, D-09)
@.planning/phases/03-vue-frontend/03-PATTERNS.md (ImportZone.vue and QueueList.vue sections)
@.planning/phases/03-vue-frontend/03-RESEARCH.md (HTML5 Drag-and-Drop section lines 629-720, Pitfall 5)
@.planning/phases/03-vue-frontend/03-UI-SPEC.md (Drag-Drop Zone Contract lines 292-318, Video Queue Item Contract lines 266-289, Copywriting)
@src/composables/useQueue.ts (useQueue composable: importVideo, removeFromQueue, clearQueue)
@src/stores/queue.ts (useQueueStore: entries, entryCount, validCount, invalidCount)
@src/composables/useFfmpeg.ts (pattern for `open()` file dialog usage)

<interfaces>
Composable API (useQueue):
- `importVideo(filepath: string): Promise<VideoEntry | null>`
- `removeFromQueue(index: number): Promise<boolean>`
- `clearQueue(): Promise<boolean>`

Store API (useQueueStore):
- `entries: Ref<VideoEntry[]>` — reactive queue list
- `entryCount: ComputedRef<number>`
- `validCount: ComputedRef<number>`
- `invalidCount: ComputedRef<number>`

Rust `remove_from_queue` takes `index: usize` — queue items identified by position, not filename.

File dialog pattern from useFfmpeg.ts:
```typescript
import { open } from '@tauri-apps/plugin-dialog';
const selected = await open({
  multiple: true,
  filters: [{ name: 'Video Files', extensions: ['mp4', 'mov', 'avi', 'mkv', 'webm', 'flv', 'wmv'] }],
});
```

Drag-drop on macOS caveat (Pitfall 5): HTML5 `(file as any).path` may not work on macOS. Tauri v2 webview should expose `path` on dropped File objects. If not, `@tauri-apps/api/window` `onDragDropEvent` is the fallback.

Naive UI components: NButton, NIcon, NText, NTag, useMessage, useDialog (for NModal confirmation)
Icons: Upload (drop zone), FolderOpen (file picker), Clapperboard (empty state), Trash2 (remove), AlertCircle (invalid), CheckCircle (valid)

i18n keys: queue.title, queue.empty, queue.emptyInstruction, queue.addVideo, queue.remove, queue.clearAll, queue.clearConfirm, queue.cleared, queue.imported, queue.removed, queue.invalid, queue.valid, import.dropHere, import.dropActive, import.supportedFormats, import.unsupportedFormat, import.noVideoStream, import.fileNotFound

Metadata formatting per UI-SPEC:
- Duration: MM:SS format (HH:MM:SS if >= 1 hour)
- Resolution: WxH (e.g., "1920x1080")
- Size: human-readable (auto unit: B/KB/MB/GB)
- Codec: uppercase (e.g., "H264")
- FPS: one decimal (e.g., "29.97")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImportZone.vue (drag-drop zone + file dialog button)</name>
  <files>src/components/queue/ImportZone.vue</files>
  <read_first>
  - src/composables/useFfmpeg.ts — `open()` file dialog pattern (lines 49-59)
  - src/composables/useQueue.ts — importVideo function signature
  - .planning/phases/03-vue-frontend/03-RESEARCH.md — HTML5 drag-and-drop code example (lines 629-720)
  - .planning/phases/03-vue-frontend/03-UI-SPEC.md — Drag-Drop Zone Contract (lines 292-318) for exact states, colors, icon sizes, and copy
  </read_first>
  <action>
  Create `src/components/queue/ImportZone.vue` implementing the drag-and-drop import zone per D-03 (HTML5 drag zone + file picker button).

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { ref } from 'vue';
  import { NButton, NIcon, NText } from 'naive-ui';
  import { Upload, FolderOpen } from 'lucide-vue-next';
  import { useMessage } from 'naive-ui';
  import { open } from '@tauri-apps/plugin-dialog';
  import { useQueue } from '@/composables/useQueue';
  import { useI18n } from 'vue-i18n';

  const { importVideo } = useQueue();
  const message = useMessage();
  const { t } = useI18n();

  const isDragging = ref(false);

  /** Supported video file extensions for file dialog filter. */
  const VIDEO_EXTENSIONS = ['mp4', 'mov', 'avi', 'mkv', 'webm', 'flv', 'wmv'];

  function onDragOver(e: DragEvent) {
    e.preventDefault();
    if (e.dataTransfer) {
      e.dataTransfer.dropEffect = 'copy';
    }
    isDragging.value = true;
  }

  function onDragLeave(e: DragEvent) {
    // Only set dragging to false if we actually left the zone (not entering a child)
    if (e.currentTarget === e.target || !(e.currentTarget as HTMLElement)?.contains(e.relatedTarget as Node)) {
      isDragging.value = false;
    }
  }

  async function onDrop(e: DragEvent) {
    e.preventDefault();
    isDragging.value = false;

    const files = e.dataTransfer?.files;
    if (!files || files.length === 0) return;

    for (let i = 0; i < files.length; i++) {
      const file = files[i];
      // Tauri v2 webview exposes absolute path on dropped File objects
      const path = (file as any).path;
      if (path) {
        await importFile(path);
      } else {
        // macOS fallback: if path not available, skip or warn
        console.warn('Dropped file has no path property (macOS may require onDragDropEvent API):', file.name);
        message.warning('Unable to get file path. Please use the "Add Videos" button instead.');
      }
    }
  }

  async function onAddFileClick() {
    const selected = await open({
      multiple: true,
      filters: [{
        name: 'Video Files',
        extensions: VIDEO_EXTENSIONS,
      }],
    });
    if (selected) {
      const paths = Array.isArray(selected) ? selected : [selected];
      for (const path of paths) {
        await importFile(path);
      }
    }
  }

  async function importFile(filepath: string) {
    const entry = await importVideo(filepath);
    if (entry) {
      message.success(t('queue.imported', { filename: entry.filename }));
    } else {
      // importVideo logs detailed error to console; show generic toast
      message.error(t('notification.operationFailed', { error: 'Import failed' }));
    }
  }
  </script>

  <template>
    <div
      class="import-zone"
      :class="{ 'import-zone--dragging': isDragging }"
      @dragover="onDragOver"
      @dragleave="onDragLeave"
      @drop="onDrop"
    >
      <NIcon :size="48" :color="isDragging ? '#2080f0' : undefined">
        <Upload />
      </NIcon>
      <NText class="mt-2">
        {{ isDragging ? t('import.dropActive') : t('import.dropHere') }}
      </NText>
      <NText depth="3" class="text-xs mt-1">
        {{ t('import.supportedFormats') }}
      </NText>
      <NButton class="mt-3" @click="onAddFileClick">
        <template #icon>
          <NIcon :size="16"><FolderOpen /></NIcon>
        </template>
        {{ t('queue.addVideo') }}
      </NButton>
    </div>
  </template>

  <style scoped>
  .import-zone {
    border: 2px dashed var(--n-border-color);
    border-radius: 8px;
    padding: 24px;
    text-align: center;
    transition: border-color 0.2s, background-color 0.2s;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 120px;
    margin: 0 16px 12px;
  }
  .import-zone--dragging {
    border-color: #2080f0;
    border-style: solid;
    background-color: rgba(32, 128, 240, 0.08);
  }
  </style>
  ```

  Key design decisions:
  - Drag zone is also clickable (the "Add Videos" button) — drop zone itself is NOT a click-to-open target
  - `onDragLeave` checks `relatedTarget` to avoid flickering when dragging over child elements
  - `(file as any).path` extracts the absolute file path that Tauri v2 webview attaches to drop File objects
  - macOS fallback: if `path` is undefined, show a warning message suggesting the "Add Videos" button
  - File dialog uses `@tauri-apps/plugin-dialog` `open()` with `multiple: true` and video extension filter
  - Each imported file shows individual success/error toast per D-10
  - `importVideo()` returns `VideoEntry | null` — success shows filename in toast, failure shows generic error
  </action>
  <verify>
    <automated>grep -c "onDragOver" src/components/queue/ImportZone.vue && grep -c "onDrop" src/components/queue/ImportZone.vue && grep -c "onAddFileClick" src/components/queue/ImportZone.vue && grep -c "importVideo" src/components/queue/ImportZone.vue && grep -c "open.*from '@tauri-apps/plugin-dialog'" src/components/queue/ImportZone.vue && grep -c "import-zone--dragging" src/components/queue/ImportZone.vue && grep -c "useMessage" src/components/queue/ImportZone.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/queue/ImportZone.vue` exists with `<script setup lang="ts">`
  - Contains HTML5 drag event handlers: `@dragover`, `@dragleave`, `@drop` on the root div
  - `onDragOver` calls `e.preventDefault()` and sets `e.dataTransfer.dropEffect = 'copy'`
  - `onDragLeave` handles child element edge case (checks `relatedTarget`)
  - `onDrop` extracts `e.dataTransfer.files`, iterates, and calls `importVideo(path)` for each file
  - `onDrop` handles macOS path fallback: if `(file as any).path` is undefined, shows warning
  - Contains `onAddFileClick()` using `open()` from `@tauri-apps/plugin-dialog` with `multiple: true` and video extension filter `['mp4', 'mov', 'avi', 'mkv', 'webm', 'flv', 'wmv']`
  - Shows success toast with `t('queue.imported', { filename })` on successful import
  - Shows error toast on import failure
  - Visual states: default (dashed border), dragging (solid `#2080f0` border + accent background)
  - Dragging state: icon color changes to `#2080f0`, text shows `t('import.dropActive')`
  - Idle text: `t('import.dropHere')`
  - Supported format text: `t('import.supportedFormats')`
  - "Add Videos" button with FolderOpen icon
  - Minimum height 120px (touch target per UI-SPEC)
  - `<style scoped>` with `.import-zone` and `.import-zone--dragging` classes
  </acceptance_criteria>
  <done>ImportZone created with HTML5 drag-drop zone, visual feedback states, and file dialog button</done>
</task>

<task type="auto">
  <name>Task 2: Create QueueList.vue (video queue table, empty state, clear-all confirmation)</name>
  <files>src/components/queue/QueueList.vue</files>
  <read_first>
  - src/components/FFmpegStatus.vue — state-driven v-if rendering pattern
  - src/components/seed/SeedList.vue — sibling component (similar list/empty state structure)
  - src/stores/queue.ts — useQueueStore (entries, entryCount, validCount, invalidCount)
  - src/composables/useQueue.ts — useQueue composable (removeFromQueue, clearQueue)
  - .planning/phases/03-vue-frontend/03-UI-SPEC.md — Video Queue Item Contract (lines 266-289) for exact layout, metadata formatting, and status tags
  </read_first>
  <action>
  Create `src/components/queue/QueueList.vue` implementing the video queue display per D-07 (guided empty state), D-08 (empty state triggers file dialog), and D-09 (NModal confirmation for clear queue).

  The component uses a manual v-for with NCard items (NOT NDataTable) for full style control per the pattern map recommendation. Each item shows filename, metadata row, status tag, and remove button.

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { ref } from 'vue';
  import { NButton, NIcon, NText, NTag, NScrollbar, useDialog, useMessage } from 'naive-ui';
  import { Clapperboard, Trash2, AlertCircle, CheckCircle, Plus } from 'lucide-vue-next';
  import { open } from '@tauri-apps/plugin-dialog';
  import { useQueueStore } from '@/stores/queue';
  import { useQueue } from '@/composables/useQueue';
  import { useI18n } from 'vue-i18n';
  import type { VideoEntry } from '@/types/video';

  const store = useQueueStore();
  const { importVideo, removeFromQueue, clearQueue } = useQueue();
  const dialog = useDialog();
  const message = useMessage();
  const { t } = useI18n();

  const VIDEO_EXTENSIONS = ['mp4', 'mov', 'avi', 'mkv', 'webm', 'flv', 'wmv'];

  /** Format duration seconds to MM:SS or HH:MM:SS. */
  function formatDuration(secs: number): string {
    if (secs < 0 || !isFinite(secs)) return '--:--';
    const h = Math.floor(secs / 3600);
    const m = Math.floor((secs % 3600) / 60);
    const s = Math.floor(secs % 60);
    if (h > 0) {
      return `${h}:${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;
    }
    return `${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;
  }

  /** Format bytes to human-readable string. */
  function formatBytes(bytes: number): string {
    if (bytes === 0) return '0 B';
    const units = ['B', 'KB', 'MB', 'GB'];
    let i = 0;
    let size = bytes;
    while (size >= 1024 && i < units.length - 1) {
      size /= 1024;
      i++;
    }
    return `${size.toFixed(i === 0 ? 0 : 1)} ${units[i]}`;
  }

  /** Format codec name to uppercase. */
  function formatCodec(codec: string): string {
    return codec.toUpperCase();
  }

  /** Format FPS to one decimal. */
  function formatFps(fps: number): string {
    return fps.toFixed(1);
  }

  /** Build metadata string: "05:32 | 1920x1080 | 128.5 MB | H264" */
  function metadataLine(entry: VideoEntry): string {
    const m = entry.metadata;
    return [
      formatDuration(m.durationSecs),
      `${m.width}x${m.height}`,
      formatBytes(m.sizeBytes),
      formatCodec(m.codec),
    ].join(' | ');
  }

  async function onRemove(index: number) {
    const ok = await removeFromQueue(index);
    if (ok) {
      message.success(t('queue.removed'));
    } else {
      message.error(t('notification.operationFailed', { error: 'Remove failed' }));
    }
  }

  function onClearAll() {
    dialog.warning({
      title: t('queue.clearAll'),
      content: t('queue.clearConfirm', { count: store.entryCount }),
      positiveText: t('queue.clearAll'),
      negativeText: t('common.back'),
      onPositiveClick: async () => {
        const ok = await clearQueue();
        if (ok) {
          message.success(t('queue.cleared'));
        } else {
          message.error(t('notification.operationFailed', { error: 'Clear failed' }));
        }
      },
    });
  }

  async function onAddVideoClick() {
    const selected = await open({
      multiple: true,
      filters: [{ name: 'Video Files', extensions: VIDEO_EXTENSIONS }],
    });
    if (selected) {
      const paths = Array.isArray(selected) ? selected : [selected];
      for (const path of paths) {
        const entry = await importVideo(path);
        if (entry) {
          message.success(t('queue.imported', { filename: entry.filename }));
        }
      }
    }
  }
  </script>

  <template>
    <div class="flex flex-col flex-1 min-h-0 px-4">
      <!-- Header: title + clear all -->
      <div class="flex items-center justify-between py-2 shrink-0">
        <NText strong class="text-sm">
          {{ t('queue.title') }} ({{ store.entryCount }})
        </NText>
        <NButton
          v-if="store.entryCount > 0"
          size="tiny"
          quaternary
          type="error"
          @click="onClearAll"
        >
          {{ t('queue.clearAll') }}
        </NButton>
      </div>

      <!-- Empty State (D-07, D-08) -->
      <div v-if="store.entryCount === 0" class="flex-1 flex items-center justify-center py-12">
        <div class="text-center">
          <NIcon :size="48" color="#2080f0">
            <Clapperboard />
          </NIcon>
          <NText depth="2" class="mt-3 block">{{ t('queue.empty') }}</NText>
          <NText depth="3" class="text-xs mt-1 block">{{ t('queue.emptyInstruction') }}</NText>
          <NButton type="primary" class="mt-4" @click="onAddVideoClick">
            <template #icon>
              <NIcon :size="16"><Plus /></NIcon>
            </template>
            {{ t('queue.addVideo') }}
          </NButton>
        </div>
      </div>

      <!-- Queue List -->
      <NScrollbar v-else class="flex-1">
        <div class="space-y-1.5 pb-4">
          <div
            v-for="(entry, index) in store.entries"
            :key="entry.filepath"
            class="queue-item"
          >
            <div class="flex items-center justify-between gap-3 py-2 px-3 rounded-md bg-[#1a1a1f]">
              <!-- File info -->
              <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2">
                  <NText strong class="truncate text-sm">{{ entry.filename }}</NText>
                  <NTag
                    :type="entry.status === 'valid' ? 'success' : 'warning'"
                    :bordered="false"
                    size="small"
                  >
                    <template #icon>
                      <NIcon :size="12">
                        <CheckCircle v-if="entry.status === 'valid'" />
                        <AlertCircle v-else />
                      </NIcon>
                    </template>
                    {{ entry.status === 'valid' ? t('queue.valid') : t('queue.invalid') }}
                  </NTag>
                </div>
                <NText depth="3" class="text-xs mt-0.5 block">
                  {{ metadataLine(entry) }}
                </NText>
              </div>

              <!-- Remove button -->
              <NButton
                size="tiny"
                quaternary
                type="error"
                @click="onRemove(index)"
              >
                <template #icon>
                  <NIcon :size="14"><Trash2 /></NIcon>
                </template>
              </NButton>
            </div>
          </div>
        </div>
      </NScrollbar>
    </div>
  </template>

  <style scoped>
  .queue-item {
    transition: background-color 0.15s;
  }
  </style>
  ```

  Key design decisions:
  - Manual v-for with div items (not NDataTable) for full style control over dark theme
  - Each item: filename (bold, truncated), status NTag (success=valid, warning=invalid), metadata line (duration | resolution | size | codec), remove Trash2 button
  - Remove button calls `removeFromQueue(index)` which matches Rust `remove_from_queue` taking `usize` index
  - Clear All uses `useDialog().warning()` (NModal) per D-09 for irreversible destructive action
  - Empty state: Clapperboard icon, descriptive text, "Add Videos" CTA calling file dialog per D-08
  - Metadata formatting: `formatDuration()` (MM:SS or HH:MM:SS), `formatBytes()` (B/KB/MB/GB), `formatCodec()` (uppercase)
  - Items use `entry.filepath` as `:key` (unique, since duplicates allowed per D-15)
  - `useDialog()` and `useMessage()` require the providers added in Plan 03-04 App.vue
  </action>
  <verify>
    <automated>grep -c "removeFromQueue" src/components/queue/QueueList.vue && grep -c "clearQueue" src/components/queue/QueueList.vue && grep -c "importVideo" src/components/queue/QueueList.vue && grep -c "useDialog" src/components/queue/QueueList.vue && grep -c "formatDuration" src/components/queue/QueueList.vue && grep -c "formatBytes" src/components/queue/QueueList.vue && grep -c "NScrollbar" src/components/queue/QueueList.vue && grep -c "entryCount" src/components/queue/QueueList.vue && grep -c "Clapperboard" src/components/queue/QueueList.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/queue/QueueList.vue` exists with `<script setup lang="ts">`
  - Header shows `t('queue.title')` with `({{ store.entryCount }})` and "Clear All" button (visible only when entries > 0)
  - "Clear All" button triggers `useDialog().warning()` with NModal confirmation: title `t('queue.clearAll')`, content `t('queue.clearConfirm', { count })`, positive button calls `clearQueue()`
  - Empty state (v-if="store.entryCount === 0"): Clapperboard icon 48px, descriptive text, "Add Videos" CTA button
  - Empty state CTA calls `onAddVideoClick()` which uses `open()` file dialog and calls `importVideo()` per D-08
  - Queue items rendered via `v-for="(entry, index) in store.entries"` with `:key="entry.filepath"`
  - Each item shows: filename (NText strong, truncated), status NTag (success/warning with CheckCircle/AlertCircle icon), metadata line (duration | resolution | size | codec), remove Trash2 button
  - `formatDuration(secs)` returns MM:SS or HH:MM:SS format
  - `formatBytes(bytes)` returns human-readable (B, KB, MB, GB)
  - `formatCodec(codec)` returns uppercase
  - Remove button calls `onRemove(index)` which calls `removeFromQueue(index)` and shows success/error toast
  - All text uses `t()` i18n function
  - NScrollbar wraps the queue item list for consistent dark theme scrollbar
  - `<style scoped>` with `.queue-item` transition
  </acceptance_criteria>
  <done>QueueList created with metadata table, empty state, clear-all NModal, and per-item remove</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML5 drag-drop → Tauri IPC | File paths extracted from browser File API. Rust `import_video` validates path existence and extension. |
| File dialog → Tauri IPC | Native dialog returns OS-validated paths. Still validated by Rust backend. |
| User click → destructive action | Clear queue requires NModal confirmation. Single remove is reversible by re-importing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Spoofing | ImportZone drag-drop path | mitigate | Rust `import_video` validates file exists at path and extension is supported. Frontend shows error toast on failure. |
| T-03-14 | Tampering | QueueList removeFromQueue index | mitigate | Rust validates index is within bounds (`0..queue.len()`). Out-of-bounds returns error. |
| T-03-15 | Repudiation | QueueList clearQueue | accept | No audit log needed for local desktop app. Clear is not a security event. |
</threat_model>

<verification>
```bash
# Verify both queue components exist with required elements
grep -c "onDrop" src/components/queue/ImportZone.vue && \
grep -c "onDragOver" src/components/queue/ImportZone.vue && \
grep -c "import-zone" src/components/queue/ImportZone.vue && \
grep -c "removeFromQueue" src/components/queue/QueueList.vue && \
grep -c "clearQueue" src/components/queue/QueueList.vue && \
grep -c "formatDuration" src/components/queue/QueueList.vue && \
echo "Queue components verified"
```
</verification>

<success_criteria>
- [ ] ImportZone.vue renders drag-drop zone with dashed border and visual feedback on dragover
- [ ] ImportZone.vue drag-over changes border to solid accent, background to accent-tinted, text to "Drop to import"
- [ ] ImportZone.vue "Add Videos" button opens native file dialog (multiple: true, video extension filter)
- [ ] ImportZone.vue shows success/error toast per imported file
- [ ] QueueList.vue renders guided empty state when queue is empty (icon + text + CTA)
- [ ] QueueList.vue renders video entries with filename, status tag, metadata line, remove button
- [ ] QueueList.vue Clear All uses NModal confirmation before calling clearQueue()
- [ ] QueueList.vue metadata formatting: duration as MM:SS, size as human-readable, codec uppercase
- [ ] Both components use useMessage() for operation feedback per D-10
- [ ] Both components use vue-i18n t() for all text
</success_criteria>

<output>
After completion, create `.planning/phases/03-vue-frontend/03-06-SUMMARY.md`
</output>
