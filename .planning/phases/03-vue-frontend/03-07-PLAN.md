---
phase: 03-vue-frontend
plan: 07
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/components/batch/BatchControls.vue
  - src/components/batch/BatchBanner.vue
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "BatchControls renders seed selector (NSelect), concurrency selector (1-4, default 1), output directory display + change button, and Start/Cancel processing button per D-11 and D-12"
    - "Output directory defaults to ~/Videos/sandwich-output/ with a 'Change' button opening native directory picker per D-12"
    - "Success/error feedback via useMessage() for batch operations per D-10"
    - "Start button triggers startBatch(seedId, outputDir) through the composable; Cancel button triggers cancelBatch()"
    - "Start button is disabled when no seed is selected or queue has no valid entries"
    - "Concurrency preference is persisted to tauri-plugin-store when changed per D-11"
    - "Seed selector, concurrency selector, and output dir button are disabled during batch processing"
    - "BatchBanner renders an aggregate progress bar (N/Total) and overall percentage, visible only when batch is processing per D-13"
  artifacts:
    - path: "src/components/batch/BatchControls.vue"
      provides: "Batch configuration and start/cancel controls"
      min_lines: 100
    - path: "src/components/batch/BatchBanner.vue"
      provides: "Batch progress banner with NProgress bar"
      min_lines: 40
  key_links:
    - from: "src/components/batch/BatchControls.vue"
      to: "src/composables/useBatch.ts"
      via: "startBatch(seedId, outputDir) and cancelBatch()"
      pattern: "startBatch"
    - from: "src/components/batch/BatchControls.vue"
      to: "src/stores/seed.ts"
      via: "seeds list for NSelect options, selectedSeedId for current selection"
      pattern: "seedStore.seeds"
    - from: "src/components/batch/BatchControls.vue"
      to: "src/stores/queue.ts"
      via: "validCount for start button enable/disable"
      pattern: "queueStore.validCount"
    - from: "src/components/batch/BatchBanner.vue"
      to: "src/stores/batch.ts"
      via: "progress, overallPercent, isProcessing"
      pattern: "batchStore.progress"
---

<objective>
Create BatchControls.vue (sticky bottom section with seed selector, output directory picker, concurrency selector, and start/cancel button) and BatchBanner.vue (processing progress banner with NProgress bar visible during batch execution). Implements D-11 (concurrency 1-4, persisted), D-12 (output directory display + change), and D-13 (progress banner structure for Phase 4).

Purpose: BatchControls is the action hub — users configure the batch (seed, output dir, concurrency) and initiate/cancel processing. BatchBanner provides the progress display structure that Phase 4 will connect to real-time events.

Output: 2 new component files in `src/components/batch/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-vue-frontend/03-CONTEXT.md (D-11, D-12, D-13)
@.planning/phases/03-vue-frontend/03-PATTERNS.md (BatchControls.vue and BatchBanner.vue sections)
@.planning/phases/03-vue-frontend/03-UI-SPEC.md (Batch Controls Contract lines 320-348, Processing State)
@src/composables/useBatch.ts (useBatch composable: startBatch, cancelBatch, getBatchStatus)
@src/stores/batch.ts (useBatchStore: progress, isProcessing, overallPercent)
@src/stores/seed.ts (useSeedStore: seeds, selectedSeedId, selectedSeed)
@src/stores/queue.ts (useQueueStore: validCount, entryCount)
@src/composables/useFfmpeg.ts (pattern for `open()` directory dialog)

<interfaces>
Composable API (useBatch):
- `startBatch(seedId: string, outputDir: string): Promise<boolean>` — NOTE: does NOT take concurrency (Rust reads from store)
- `cancelBatch(): Promise<boolean>`
- `getBatchStatus(): Promise<BatchProgress | null>`

Store APIs:
- useSeedStore: `seeds: Ref<Seed[]>`, `selectedSeedId: Ref<string | null>`, `selectedSeed: ComputedRef<Seed | null>`
- useQueueStore: `validCount: ComputedRef<number>`, `entryCount: ComputedRef<number>`
- useBatchStore: `progress: Ref<BatchProgress>`, `isProcessing: Ref<boolean>`, `overallPercent: ComputedRef<number>`

IPC note: `start_batch` in Rust takes `seed_id: String, output_dir: String` only. Concurrency is read from `sandwich-config.json` key `concurrency` by `get_concurrency_preference()`. Frontend writes concurrency to store via `@tauri-apps/plugin-store` `store.set('concurrency', n)`.

Directory dialog pattern (from useFfmpeg.ts):
```typescript
import { open } from '@tauri-apps/plugin-dialog';
const dir = await open({ directory: true, multiple: false, title: 'Select Output Directory' });
```

Plugin-store pattern (Rust uses `app.store("sandwich-config.json")`):
```typescript
import { Store } from '@tauri-apps/plugin-store';
const store = new Store('sandwich-config.json');
await store.set('concurrency', value);
await store.save();
```

D-12 default output directory: `~/Videos/sandwich-output/`
D-11 concurrency: NSelect options [1, 2, 3, 4], default 1, persisted

Naive UI components: NSelect, NButton, NText, NIcon, NProgress, NSpace
Icons: Play (start), Square (cancel), FolderOpen (output dir), GripVertical (could use for handle but not needed)

i18n keys: batch.title, batch.selectSeed, batch.concurrency, batch.outputDir, batch.changeDir, batch.start, batch.cancel, batch.cancelConfirm, batch.noSeedSelected, batch.queueEmpty, batch.alreadyRunning, batch.processing, batch.progress, batch.defaultOutputDir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BatchControls.vue (seed selector, output dir, concurrency, start/cancel)</name>
  <files>src/components/batch/BatchControls.vue</files>
  <read_first>
  - src/components/FFmpegDownload.vue — NSelect, NButton pattern, state-dependent rendering
  - src/composables/useBatch.ts — startBatch, cancelBatch function signatures
  - src/stores/seed.ts — useSeedStore for seed selection options
  - src/stores/queue.ts — useQueueStore for validCount (enable/disable start)
  - src/stores/batch.ts — useBatchStore for isProcessing state
  - src/composables/useFfmpeg.ts — `open({ directory: true })` file dialog pattern
  </read_first>
  <action>
  Create `src/components/batch/BatchControls.vue` implementing the batch processing controls per D-11 (concurrency selector, persisted), D-12 (output directory display + change), and the UI-SPEC Batch Controls Contract.

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { ref, computed, onMounted } from 'vue';
  import { NSelect, NButton, NIcon, NText, NSpace } from 'naive-ui';
  import { Play, Square, FolderOpen } from 'lucide-vue-next';
  import { useMessage } from 'naive-ui';
  import { open } from '@tauri-apps/plugin-dialog';
  import { Store } from '@tauri-apps/plugin-store';
  import { useSeedStore } from '@/stores/seed';
  import { useQueueStore } from '@/stores/queue';
  import { useBatchStore } from '@/stores/batch';
  import { useBatch } from '@/composables/useBatch';
  import { useI18n } from 'vue-i18n';

  const seedStore = useSeedStore();
  const queueStore = useQueueStore();
  const batchStore = useBatchStore();
  const { startBatch, cancelBatch } = useBatch();
  const message = useMessage();
  const { t } = useI18n();

  const concurrency = ref<number>(1);
  const outputDir = ref<string>('');
  const storeLoaded = ref(false);

  /** Seed options for NSelect: value=id, label=alias */
  const seedOptions = computed(() =>
    seedStore.seeds.map(s => ({
      label: s.alias,
      value: s.id,
    })),
  );

  /** Start disabled when no seed selected or queue has no valid entries. */
  const startDisabled = computed(() =>
    !seedStore.selectedSeedId || queueStore.validCount === 0,
  );

  /** Load persisted preferences from tauri-plugin-store on mount. */
  async function loadPreferences() {
    try {
      const store = new Store('sandwich-config.json');
      const savedConcurrency = await store.get<number>('concurrency');
      if (savedConcurrency && [1, 2, 3, 4].includes(savedConcurrency)) {
        concurrency.value = savedConcurrency;
      }
      const savedOutputDir = await store.get<string>('output_dir');
      if (savedOutputDir) {
        outputDir.value = savedOutputDir;
      } else {
        // Default per D-12: ~/Videos/sandwich-output/
        outputDir.value = t('batch.defaultOutputDir');
      }
    } catch (err) {
      console.warn('Failed to load batch preferences:', err);
      outputDir.value = t('batch.defaultOutputDir');
    }
    storeLoaded.value = true;
  }

  /** Persist concurrency choice to tauri-plugin-store per D-11. */
  async function onConcurrencyChange(value: number) {
    concurrency.value = value;
    try {
      const store = new Store('sandwich-config.json');
      await store.set('concurrency', value);
      await store.save();
    } catch (err) {
      console.error('Failed to save concurrency preference:', err);
    }
  }

  /** Open native directory picker for output directory per D-12. */
  async function onChangeOutputDir() {
    const dir = await open({
      directory: true,
      multiple: false,
      title: t('batch.outputDir'),
    });
    if (dir && typeof dir === 'string') {
      outputDir.value = dir;
      message.success(t('batch.changeDir'));
      // Persist output dir
      try {
        const store = new Store('sandwich-config.json');
        await store.set('output_dir', dir);
        await store.save();
      } catch (err) {
        console.error('Failed to persist output directory:', err);
      }
    }
  }

  /** Start batch processing. */
  async function onStart() {
    if (!seedStore.selectedSeedId) {
      message.warning(t('batch.noSeedSelected'));
      return;
    }
    if (queueStore.validCount === 0) {
      message.warning(t('batch.queueEmpty'));
      return;
    }
    if (batchStore.isProcessing) {
      message.warning(t('batch.alreadyRunning'));
      return;
    }

    // Resolve output dir: replace ~ with HOME
    let resolvedDir = outputDir.value;
    if (resolvedDir.startsWith('~/')) {
      // In Tauri, Rust resolves ~ automatically. We pass as-is.
    }

    const ok = await startBatch(seedStore.selectedSeedId, resolvedDir);
    if (ok) {
      // Progress will come via batch-progress events
    } else {
      message.error(t('notification.operationFailed', { error: 'Batch start failed' }));
    }
  }

  /** Cancel in-progress batch. */
  async function onCancel() {
    const ok = await cancelBatch();
    if (!ok) {
      message.error(t('notification.operationFailed', { error: 'Cancel failed' }));
    }
  }

  // Concurrency options per D-11
  const concurrencyOptions = [
    { label: '1', value: 1 },
    { label: '2', value: 2 },
    { label: '3', value: 3 },
    { label: '4', value: 4 },
  ];

  onMounted(() => {
    loadPreferences();
  });
  </script>

  <template>
    <div class="px-4 py-3">
      <NText strong class="text-sm block mb-3">{{ t('batch.title') }}</NText>

      <NSpace vertical :size="12">
        <!-- Seed Selector -->
        <NSelect
          :value="seedStore.selectedSeedId"
          :options="seedOptions"
          :placeholder="t('batch.selectSeed')"
          :disabled="batchStore.isProcessing"
          filterable
          clearable
          @update:value="(v: string | null) => seedStore.selectSeed(v)"
        />

        <!-- Concurrency (D-11) -->
        <NSelect
          :value="concurrency"
          :options="concurrencyOptions"
          :placeholder="t('batch.concurrency')"
          :disabled="batchStore.isProcessing"
          @update:value="(v: number) => onConcurrencyChange(v)"
        />

        <!-- Output Directory (D-12) -->
        <div class="flex items-center gap-2">
          <NText depth="3" class="text-xs flex-1 truncate">
            {{ outputDir }}
          </NText>
          <NButton
            size="small"
            quaternary
            :disabled="batchStore.isProcessing"
            @click="onChangeOutputDir"
          >
            <template #icon>
              <NIcon :size="14"><FolderOpen /></NIcon>
            </template>
            {{ t('batch.changeDir') }}
          </NButton>
        </div>

        <!-- Start / Cancel Button -->
        <NButton
          v-if="!batchStore.isProcessing"
          type="primary"
          size="large"
          block
          :disabled="startDisabled"
          @click="onStart"
        >
          <template #icon>
            <NIcon :size="18"><Play /></NIcon>
          </template>
          {{ t('batch.start') }}
        </NButton>
        <NButton
          v-else
          type="error"
          size="large"
          block
          @click="onCancel"
        >
          <template #icon>
            <NIcon :size="18"><Square /></NIcon>
          </template>
          {{ t('batch.cancel') }}
        </NButton>
      </NSpace>
    </div>
  </template>
  ```

  Key design decisions:
  - `start_batch` does NOT take concurrency as argument — Rust reads it from `sandwich-config.json` via `get_concurrency_preference()`
  - Concurrency is written to plugin-store on change via `store.set('concurrency', value)`
  - Output directory is written to plugin-store as `output_dir` (matching Rust's `persist_output_dir()` key)
  - Start button disabled when: no seed selected OR queue `validCount === 0`
  - During batch processing: seed selector, concurrency selector, and output dir button are all disabled
  - Start button transforms to "Cancel Processing" error-type button when batch is running
  - Preferences loaded from plugin-store on mount with defaults: concurrency=1, output_dir from i18n default
  - Seed selector uses NSelect with `filterable` and `clearable` for UX
  </action>
  <verify>
    <automated>grep -c "startBatch" src/components/batch/BatchControls.vue && grep -c "cancelBatch" src/components/batch/BatchControls.vue && grep -c "NSelect" src/components/batch/BatchControls.vue && grep -c "concurrency" src/components/batch/BatchControls.vue && grep -c "outputDir" src/components/batch/BatchControls.vue && grep -c "Store.*from '@tauri-apps/plugin-store'" src/components/batch/BatchControls.vue && grep -c "isProcessing" src/components/batch/BatchControls.vue && grep -c "startDisabled" src/components/batch/BatchControls.vue && grep -c "onConcurrencyChange" src/components/batch/BatchControls.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/batch/BatchControls.vue` exists with `<script setup lang="ts">`
  - Imports `Store` from `@tauri-apps/plugin-store` for preference persistence
  - Imports `open` from `@tauri-apps/plugin-dialog` for directory picker
  - NSelect for seed selection: options from `seedStore.seeds` (value=id, label=alias), bound to `seedStore.selectedSeedId`, filterable, clearable
  - NSelect for concurrency: options [{label:'1',value:1}, {label:'2',value:2}, {label:'3',value:3}, {label:'4',value:4}], default 1
  - Concurrency change calls `onConcurrencyChange(v)` which writes to plugin-store `sandwich-config.json` key `concurrency`
  - Output directory: NText showing current path (truncated) + "Change" NButton with FolderOpen icon
  - "Change" button calls `open({ directory: true, multiple: false })` and persists result to plugin-store key `output_dir`
  - Start button (type="primary", size="large", block, Play icon) visible when NOT processing
  - Start button disabled when `!seedStore.selectedSeedId || queueStore.validCount === 0`
  - Cancel button (type="error", size="large", block, Square icon) visible when processing
  - During processing (`batchStore.isProcessing`): seed selector, concurrency selector, and output dir button all have `:disabled="batchStore.isProcessing"`
  - Start calls `startBatch(seedId, outputDir)` — does NOT pass concurrency as argument
  - Cancel calls `cancelBatch()`
  - `onMounted` loads preferences from plugin-store: `concurrency` (default 1), `output_dir` (default from i18n `batch.defaultOutputDir`)
  - All text uses `t()` i18n function
  </acceptance_criteria>
  <done>BatchControls created with seed selector, concurrency persistence, output dir picker, and start/cancel toggle</done>
</task>

<task type="auto">
  <name>Task 2: Create BatchBanner.vue (progress banner with NProgress)</name>
  <files>src/components/batch/BatchBanner.vue</files>
  <read_first>
  - src/components/FFmpegDownload.vue — NProgress usage pattern (percentage, indicator-placement)
  - src/stores/batch.ts — useBatchStore (progress, overallPercent, isProcessing)
  </read_first>
  <action>
  Create `src/components/batch/BatchBanner.vue` implementing the batch progress display per D-13 (top banner showing processed N/Total M with aggregate progress bar). This renders only during batch processing (controlled by parent via `v-if="batchStore.isProcessing"` in MainLayout.vue).

  Exact implementation:
  ```html
  <script setup lang="ts">
  import { NProgress, NText, NSpace } from 'naive-ui';
  import { useBatchStore } from '@/stores/batch';
  import { useI18n } from 'vue-i18n';

  const batchStore = useBatchStore();
  const { t } = useI18n();
  </script>

  <template>
    <div class="batch-banner">
      <NSpace align="center" :size="12">
        <NText depth="2" class="text-xs shrink-0">
          {{ t('batch.processing') }}
        </NText>
        <NProgress
          type="line"
          :percentage="batchStore.overallPercent"
          :indicator-placement="'inside'"
          :height="24"
          :color="batchStore.overallPercent === 100 ? '#18a058' : '#2080f0'"
          class="flex-1"
        />
        <NText depth="2" class="text-xs shrink-0 tabular-nums">
          {{ t('batch.progress', {
            completed: batchStore.progress.completed,
            total: batchStore.progress.total,
          }) }}
        </NText>
      </NSpace>
    </div>
  </template>

  <style scoped>
  .batch-banner {
    background-color: #1a1a24;
    padding: 10px 16px;
    border-bottom: 1px solid #2a2a36;
  }
  </style>
  ```

  Key design decisions:
  - Banner layout: "Processing" label | NProgress bar | "completed/total" counter
  - Progress bar uses `indicator-placement="inside"` for clean inline percentage display
  - Color: `#2080f0` (accent) during processing, `#18a058` (success) when 100%
  - `tabular-nums` UnoCSS class ensures the counter numbers don't jump as digits change
  - Banner is a thin horizontal strip — dark background with bottom border for visual separation from queue list
  - This component does NOT listen for events directly — it reads reactive state from `useBatchStore` which is updated by the `useBatch` composable's event listeners
  - Phase 3: progress updates come from `start_batch` command return (which calls `get_batch_status`). Phase 4 will connect real-time `batch-progress` events via the same store.
  </action>
  <verify>
    <automated>grep -c "NProgress" src/components/batch/BatchBanner.vue && grep -c "batchStore.overallPercent" src/components/batch/BatchBanner.vue && grep -c "batchStore.progress" src/components/batch/BatchBanner.vue && grep -c "batch.processing" src/components/batch/BatchBanner.vue && grep -c "batch.progress" src/components/batch/BatchBanner.vue && grep -c "indicator-placement" src/components/batch/BatchBanner.vue && grep -c "batch-banner" src/components/batch/BatchBanner.vue</automated>
  </verify>
  <acceptance_criteria>
  - `src/components/batch/BatchBanner.vue` exists with `<script setup lang="ts">`
  - Reads from `useBatchStore()`: `progress`, `overallPercent`
  - Renders NProgress with `type="line"`, `:percentage="batchStore.overallPercent"`, `indicator-placement="inside"`, `:height="24"`
  - NProgress color: `#2080f0` when percent < 100, `#18a058` when percent === 100
  - Shows "Processing" label (NText, depth="2", text-xs)
  - Shows "{completed}/{total}" counter (NText, depth="2", text-xs, tabular-nums)
  - Layout uses NSpace with align="center" and :size="12"
  - Scoped style: `.batch-banner` with `background-color: #1a1a24`, `padding: 10px 16px`, `border-bottom: 1px solid #2a2a36`
  - All text uses `t()` i18n function
  - Component is pure display (no buttons, no event handlers, no store mutations)
  </acceptance_criteria>
  <done>BatchBanner created with processing label, NProgress bar, and completed/total counter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin-store read/write | Concurrency and output_dir preferences written to `sandwich-config.json`. Rust reads same file for concurrency. |
| User input → batch start | seedId and outputDir passed to Rust `start_batch` which validates seed exists, queue non-empty, batch not already running. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering | Plugin-store concurrency value | accept | Rust `get_concurrency_preference()` clamps to 1-4 range. Frontend NSelect limits to those values. |
| T-03-17 | Denial of Service | Rapid start/cancel toggling | mitigate | Rust `start_batch` checks `batch_state.status != Idle` and returns error if already running. Cancel sets cancel flag atomically. |
| T-03-18 | Information Disclosure | Output directory path in UI | accept | Local desktop app. Path is user's own filesystem directory. No sensitive data. |
</threat_model>

<verification>
```bash
# Verify both batch components exist with required elements
grep -c "startBatch" src/components/batch/BatchControls.vue && \
grep -c "Store" src/components/batch/BatchControls.vue && \
grep -c "NSelect" src/components/batch/BatchControls.vue && \
grep -c "NProgress" src/components/batch/BatchBanner.vue && \
grep -c "batchStore.overallPercent" src/components/batch/BatchBanner.vue && \
echo "Batch components verified"
```
</verification>

<success_criteria>
- [ ] BatchControls.vue renders seed NSelect (from seedStore), concurrency NSelect (1-4, persisted), output dir display + change button, start/cancel button
- [ ] BatchControls.vue start button disabled when no seed selected or no valid queue entries
- [ ] BatchControls.vue concurrency changes persist to tauri-plugin-store key `concurrency`
- [ ] BatchControls.vue output dir changes via native directory picker and persists to key `output_dir`
- [ ] BatchControls.vue transforms to cancel button during batch processing
- [ ] BatchControls.vue all controls disabled during batch processing except cancel
- [ ] BatchBanner.vue renders NProgress bar with inside indicator showing percentage
- [ ] BatchBanner.vue renders "Processing" label and "completed/total" counter
- [ ] BatchBanner.vue NProgress color changes to success green at 100%
- [ ] Both components use vue-i18n t() for all text
</success_criteria>

<output>
After completion, create `.planning/phases/03-vue-frontend/03-07-SUMMARY.md`
</output>
