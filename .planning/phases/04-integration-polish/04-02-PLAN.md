---
phase: 04-integration-polish
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/types/batch.ts
  - src/locales/en.json
  - src/locales/zh-CN.json
autonomous: true
requirements:
  - BATCH-02
  - BATCH-05

must_haves:
  truths:
    - "PerFileProgress TypeScript interface exists with all 6 fields matching the Rust struct"
    - "All 18 new i18n keys exist in both en.json and zh-CN.json under batch.* and batch.summary.* namespaces"
    - "The deprecated batch.cancelConfirm key is replaced by batch.cancelConfirmTitle and batch.cancelConfirmBody"
  artifacts:
    - path: "src/types/batch.ts"
      provides: "PerFileProgress TypeScript interface"
      contains: "export interface PerFileProgress"
    - path: "src/locales/en.json"
      provides: "English batch.* and batch.summary.* keys"
      contains: "batch.summary.completeTitle"
    - path: "src/locales/zh-CN.json"
      provides: "Chinese batch.* and batch.summary.* keys"
      contains: "batch.summary.completeTitle"
  key_links:
    - from: "src/types/batch.ts PerFileProgress"
      to: "src-tauri/src/models/batch.rs PerFileProgress"
      via: "camelCase field name match (serde rename_all)"
      pattern: "interface PerFileProgress"
    - from: "src/locales/en.json batch.summary.*"
      to: "src/components/batch/BatchSummary.vue"
      via: "vue-i18n $t() calls"
      pattern: "batch\\.summary\\."
---

<objective>
Add the TypeScript PerFileProgress interface matching the Rust struct and 17 new i18n keys across both locale files to form the contract for Wave 3-4 components.

Purpose: The frontend needs a PerFileProgress type for the batch-file-progress event listener and i18n keys for all new UI states (cancelling, per-file progress labels, completion summary).

Output: Updated src/types/batch.ts with PerFileProgress interface; updated en.json and zh-CN.json with 17 new keys under batch.* and batch.summary.*, deprecated cancelConfirm key replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-integration-polish/04-RESEARCH.md
@.planning/phases/04-integration-polish/04-UI-SPEC.md
@src/types/batch.ts
@src/locales/en.json
@src/locales/zh-CN.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PerFileProgress TypeScript interface to types/batch.ts</name>
  <files>src/types/batch.ts</files>
  <read_first>src/types/batch.ts</read_first>
  <action>
Add the PerFileProgress interface to src/types/batch.ts after the FileResult interface (after line 20). The interface mirrors the Rust PerFileProgress struct in src-tauri/src/models/batch.rs.

Add this code after line 20 (after FileResult's closing }):

```typescript
/** Per-file frame-level progress from the Rust executor via batch-file-progress event.
 *  Mirrors Rust struct PerFileProgress in src-tauri/src/models/batch.rs */
export interface PerFileProgress {
  /** Display filename being processed. */
  file: string;
  /** Percentage complete for this file (0-100). */
  percent: number;
  /** Current frame number being encoded (from ffmpeg-sidecar FfmpegProgress.frame). */
  currentFrame: number;
  /** Total frames in this file (duration_secs * fps from ffprobe metadata). */
  totalFrames: number;
  /** Frames per second encoding speed. */
  fps: number;
  /** Estimated remaining seconds for this file (clamped to >= 0). */
  remainingSeconds: number;
}
```

No imports needed -- standalone interface with only primitive fields.
</action>
<acceptance_criteria>
- grep -c "export interface PerFileProgress" src/types/batch.ts returns 1
- grep -c "currentFrame: number" src/types/batch.ts returns 1
- grep -c "remainingSeconds: number" src/types/batch.ts returns 1
- grep -c "fps: number" src/types/batch.ts returns 1
- npx vue-tsc -b --noEmit exits 0 with no batch.ts type errors
</acceptance_criteria>
  <verify>
    <automated>npx vue-tsc -b --noEmit 2>&1 | grep "batch.ts" | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <done>PerFileProgress TypeScript interface exists in types/batch.ts with all 6 fields, compiles cleanly with vue-tsc.</done>
</task>

<task type="auto">
  <name>Task 2: Add new i18n keys to both locale files, deprecate batch.cancelConfirm</name>
  <files>src/locales/en.json, src/locales/zh-CN.json</files>
  <read_first>src/locales/en.json</read_first>
  <action>
Add 17 new i18n keys to both locale files. Replace the existing batch.cancelConfirm key with two new keys. Add all new keys under batch.summary.* namespace.

**English (en.json)** -- In the "batch" object:

1. Replace "cancelConfirm": "Cancel batch processing? Completed files will be preserved." with:
```json
"cancelConfirmTitle": "Cancel Batch Processing?",
"cancelConfirmBody": "Completed files will be preserved. In-progress files will be discarded.",
```

2. Add these keys after "cancelConfirmBody" (before "noSeedSelected"):
```json
"keepProcessing": "Keep Processing",
"cancelling": "Cancelling...",
"processingFile": "Processing: {filename}",
"fileProgress": "Frame {current} / {total}",
"fileEta": "~{minutes} min remaining",
"fileFailed": "{filename} failed: {error}",
"noOutputDir": "Please select an output directory first",
```

3. After the existing "defaultOutputDir" key, add a new "summary" object:
```json
"summary": {
  "completeTitle": "Batch Complete",
  "cancelledTitle": "Batch Cancelled",
  "succeededSection": "Succeeded ({count})",
  "failedSection": "Failed ({count})",
  "outputPath": "{filename} -> {outputPath}",
  "fileError": "{filename}: {errorMessage}",
  "clearResults": "Clear Results",
  "completeBody": "{succeeded} succeeded, {failed} failed out of {total} files",
  "cancelledBody": "Cancelled after {succeeded} succeeded, {failed} failed out of {total} files"
}
```

**Chinese (zh-CN.json)** -- Mirror in the "batch" object:

1. Replace "cancelConfirm" with:
```json
"cancelConfirmTitle": "取消批处理？",
"cancelConfirmBody": "已完成处理的文件将保留，正在处理的文件将被丢弃。",
```

2. Add after "cancelConfirmBody":
```json
"keepProcessing": "继续处理",
"cancelling": "取消中...",
"processingFile": "正在处理：{filename}",
"fileProgress": "帧 {current} / {total}",
"fileEta": "预计剩余 {minutes} 分钟",
"fileFailed": "{filename} 处理失败：{error}",
"noOutputDir": "请先选择输出目录",
```

3. Add "summary" object:
```json
"summary": {
  "completeTitle": "批处理完成",
  "cancelledTitle": "批处理已取消",
  "succeededSection": "成功（{count}）",
  "failedSection": "失败（{count}）",
  "outputPath": "{filename} → {outputPath}",
  "fileError": "{filename}：{errorMessage}",
  "clearResults": "清除结果",
  "completeBody": "共 {total} 个文件，成功 {succeeded} 个，失败 {failed} 个",
  "cancelledBody": "已取消，共 {total} 个文件，成功 {succeeded} 个，失败 {failed} 个"
}
```

Net change: +17 keys in the batch namespace per locale. Existing keys batch.processing and batch.progress are unchanged. Unused keys batch.completed and batch.cancelled are kept as-is.
</action>
<acceptance_criteria>
- grep -c '"cancelConfirm":' src/locales/en.json returns 0 (old single key removed)
- grep -c '"cancelConfirmTitle"' src/locales/en.json returns 1
- grep -c '"cancelConfirmBody"' src/locales/en.json returns 1
- grep -c '"keepProcessing"' src/locales/en.json returns 1
- grep -c '"cancelling"' src/locales/en.json returns 1
- grep -c '"processingFile"' src/locales/en.json returns 1
- grep -c '"fileProgress"' src/locales/en.json returns 1
- grep -c '"fileEta"' src/locales/en.json returns 1
- grep -c '"fileFailed"' src/locales/en.json returns 1
- grep -c '"noOutputDir"' src/locales/en.json returns 1
- grep -c '"completeTitle"' src/locales/en.json returns 1
- grep -c '"cancelledTitle"' src/locales/en.json returns 1
- grep -c '"succeededSection"' src/locales/en.json returns 1
- grep -c '"failedSection"' src/locales/en.json returns 1
- grep -c '"clearResults"' src/locales/en.json returns 1
- grep -c '"completeBody"' src/locales/en.json returns 1
- grep -c '"cancelledBody"' src/locales/en.json returns 1
- Every key above also exists in zh-CN.json with the same count
- Both locale files are valid JSON: node -e "JSON.parse(require('fs').readFileSync('...','utf8'))" exits 0
</acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/locales/en.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8'))" && echo "BOTH_VALID_JSON"</automated>
  </verify>
  <done>Both locale files contain all 17 new i18n keys under batch.* and batch.summary.*, the deprecated cancelConfirm key is removed, and both files are valid JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n key interpolation | Template variables like {filename} are filled from Rust event payloads (controlled by the backend, not user input). No XSS risk in vue-i18n template interpolation -- it escapes by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | i18n JSON files | accept | JSON files are in the source tree, not writable at runtime. No runtime tampering surface. |
| T-04-04 | Information Disclosure | i18n keys | accept | All keys are UI-facing localization strings. No secrets or credentials. |
</threat_model>

<verification>
- All 17 new keys present in both locale files
- batch.cancelConfirm (old single key) replaced by batch.cancelConfirmTitle + batch.cancelConfirmBody
- Both files are valid JSON
- npx vue-tsc -b --noEmit passes (no TypeScript errors from missing types or i18n key issues)
</verification>

<success_criteria>
The TypeScript PerFileProgress interface and 17 new i18n keys form the contract that Wave 3 (store/composable) and Wave 4 (components) will build against. Both locales are complete and valid JSON.
</success_criteria>

<output>
After completion, create .planning/phases/04-integration-polish/04-02-SUMMARY.md
</output>
