---
phase: 06
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/seed.ts
  - src/types/video.ts
  - src/types/batch.ts
  - src/types/log.ts
  - src/locales/zh-CN.json
  - src/locales/en.json
autonomous: true
requirements:
  - D-02
  - D-07
  - D-10
  - D-11
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
must_haves:
  truths:
    - "TypeScript OperationType union contains all 20 variants matching Rust serde camelCase"
    - "Seed interface has optional strengthTier field"
    - "VideoEntry interface has optional thumbnailBase64 and orderIndex fields"
    - "ProcessingLogEntry interface exists in new types/log.ts"
    - "Both locale files contain ~50 new i18n keys for all Phase 6 UI text"
  artifacts:
    - path: "src/types/seed.ts"
      provides: "Extended Seed interface + OperationType union type (20 members)"
      contains: "hueRotate"
    - path: "src/types/video.ts"
      provides: "VideoEntry with thumbnailBase64 and orderIndex"
      contains: "thumbnailBase64"
    - path: "src/types/log.ts"
      provides: "ProcessingLogEntry interface"
      contains: "export interface ProcessingLogEntry"
    - path: "src/locales/zh-CN.json"
      provides: "Chinese translations for all new features"
    - path: "src/locales/en.json"
      provides: "English translations for all new features"
  key_links:
    - from: "src/types/seed.ts (OperationType)"
      to: "src-tauri/src/models/seed.rs (OperationType)"
      via: "serde camelCase renaming convention"
      pattern: "'hueRotate' matches Rust HueRotate -> camelCase -> hueRotate"
    - from: "src/types/log.ts"
      to: "src/stores/log.ts"
      via: "import for Pinia store typing"
      pattern: "import type { ProcessingLogEntry } from '@/types/log'"
---

<objective>
Mirror all Rust model changes in TypeScript type definitions and add all ~50 new i18n keys to both locale files. This provides the frontend with complete type safety and translation coverage before any component work begins.

Purpose: Frontend code that follows this plan can reference exact types and i18n keys before stores, composables, and components are built. The full i18n key set means components never block on missing translations.

Output: Three modified type files, one new type file, two modified locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-/06-CONTEXT.md
@.planning/phases/06-/06-RESEARCH.md
@.planning/phases/06-/06-PATTERNS.md (sections 16-19)
@.planning/phases/06-/06-UI-SPEC.md (Copywriting Contract section)

<interfaces>
From src/types/seed.ts (existing, lines 1-24):
```typescript
export interface Seed {
  id: string; alias: string; operations: Operation[]; createdAt: string;
}
export interface Operation {
  opType: OperationType; startFrame: number; durationFrames: number;
  params: Record<string, unknown>;
}
export type OperationType =
  | 'mathOverlay' | 'pixelShift' | 'frameDrop'
  | 'gopModify' | 'metadataErase' | 'audioTweak' | 'remux';
```

From src/types/video.ts (existing, lines 1-21):
```typescript
export interface VideoEntry {
  filename: string; filepath: string; metadata: VideoMetadata; status: VideoStatus;
}
```

From src/types/batch.ts (existing, lines 1-49): Contains BatchProgress, BatchResult, FileSuccess, FileResult, PerFileProgress interfaces.

From src/locales/en.json: Key structure uses nested objects: "seed": { "title": "...", ... }, "batch": { "title": "...", ... }, "queue": { "title": "...", ... }, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TypeScript type definitions (seed.ts, video.ts, batch.ts, new log.ts)</name>
  <files>src/types/seed.ts, src/types/video.ts, src/types/batch.ts, src/types/log.ts</files>
  <read_first>src/types/seed.ts, src/types/video.ts, src/types/batch.ts</read_first>

  <action>
**File 1: `src/types/seed.ts`** — Two changes:

1. Extend `OperationType` union type (line 17-24). Append the 13 new variants, preserving alphabetical-ish grouping. The union becomes 20 members total:
```typescript
export type OperationType =
  | 'mathOverlay' | 'pixelShift' | 'frameDrop'
  | 'gopModify' | 'metadataErase' | 'audioTweak' | 'remux'
  // Phase 6: Color processing (4)
  | 'hueRotate' | 'saturationAdjust' | 'brightnessContrast' | 'colorBalance'
  // Phase 6: Noise texture (3)
  | 'filmGrain' | 'gaussianBlur' | 'sharpen'
  // Phase 6: Geometric fine-tuning (3)
  | 'microRotate' | 'tinyScale' | 'flip'
  // Phase 6: Blend overlay (3)
  | 'solidColorOverlay' | 'gradientOverlay' | 'watermarkBlend';
```

2. Extend `Seed` interface (line 3-8). Add optional `strengthTier` field after `createdAt`:
```typescript
export interface Seed {
  id: string;
  alias: string;
  operations: Operation[];
  createdAt: string;
  /** Strength tier used when generating this seed (D-07). Optional for backward compat with old seeds. */
  strengthTier?: 'conservative' | 'standard' | 'aggressive';
}
```

Keep `Operation` interface unchanged. Keep existing comments.

**File 2: `src/types/video.ts`** — Extend `VideoEntry` interface. Add two optional fields after `status`:
```typescript
export interface VideoEntry {
  filename: string;
  filepath: string;
  metadata: VideoMetadata;
  status: VideoStatus;
  /** Base64-encoded JPEG thumbnail (first frame, 120px wide). Undefined if extraction failed (D-15). */
  thumbnailBase64?: string;
  /** 0-based display order for drag-and-drop reordering (D-14). */
  orderIndex?: number;
}
```

Keep `VideoMetadata` and `VideoStatus` types unchanged.

**File 3: `src/types/batch.ts`** — No changes needed to existing types. The `ProcessingLogEntry` is in its own file.

**File 4: `src/types/log.ts`** — CREATE NEW FILE:
```typescript
// Mirrors Rust struct ProcessingLogEntry in src-tauri/src/models/batch.rs
// All field names use camelCase matching #[serde(rename_all = "camelCase")]

/** Processing log entry persisted to store for the history panel (D-16 / PROD-03).
 *  Accumulated in Pinia logStore; capped at 500 most recent entries. */
export interface ProcessingLogEntry {
  /** Unique identifier (UUID v4). */
  id: string;
  /** ISO 8601 timestamp of processing completion. */
  timestamp: string;
  /** Source filename (not full path). */
  file: string;
  /** Seed alias used for this processing run. */
  seedAlias: string;
  /** "success" or "failure". */
  status: 'success' | 'failure';
  /** MD5 hash before processing (hex string). */
  md5Before: string;
  /** MD5 hash after processing (hex string). */
  md5After: string;
  /** true if md5Before !== md5After (file was modified by processing). */
  modified: boolean;
  /** Output file path, or null if processing failed before output. */
  outputPath: string | null;
  /** Error message, or null if processing succeeded. */
  errorMessage: string | null;
  /** Processing duration in milliseconds. */
  durationMs: number;
}
```

**Validation:** Run `vue-tsc -b` to verify no type errors. The new types are standalone — no imports to update.
  </action>

  <verify>
    <automated>cd /Users/ghost/Code/sandwich && npx vue-tsc -b 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "hueRotate" src/types/seed.ts` returns 1
    - `grep -c "watermarkBlend" src/types/seed.ts` returns 1
    - `grep -c "strengthTier" src/types/seed.ts` returns at least 1
    - `grep -c "thumbnailBase64" src/types/video.ts` returns at least 1
    - `grep -c "orderIndex" src/types/video.ts` returns at least 1
    - `grep -c "export interface ProcessingLogEntry" src/types/log.ts` returns 1
    - `grep -c "durationMs" src/types/log.ts` returns at least 1
    - Total OperationType union members: `grep -o "'.*'" src/types/seed.ts | grep -c "'"` shows at least 20 variations
    - `cd /Users/ghost/Code/sandwich && npx vue-tsc -b` exits 0
  </acceptance_criteria>

  <done>All 4 type files are correct. OperationType has 20 members. Seed has strengthTier. VideoEntry has thumbnailBase64 and orderIndex. ProcessingLogEntry type exists. vue-tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add ~50 new i18n keys to zh-CN.json and en.json</name>
  <files>src/locales/zh-CN.json, src/locales/en.json</files>
  <read_first>src/locales/zh-CN.json, src/locales/en.json</read_first>

  <action>
Add new i18n keys to BOTH locale files. Keys are organized under existing top-level sections (`seed`, `batch`, `queue`) plus one new top-level section (`log`).

Insert each group within its existing section (e.g., `seed.export` goes inside the `"seed"` object). The `"log"` section is new and goes at the top level alongside existing sections.

**zh-CN.json — new keys to add:**

Under `"seed"` (inside existing seed object):
```json
"strengthTier": "强度档位",
"strength": {
  "conservative": "保守",
  "standard": "标准",
  "aggressive": "激进"
},
"export": "导出",
"import": "导入",
"exported": "种子已导出",
"imported": "种子「{alias}」已导入",
"importFailed": "导入失败：{error}",
"importInvalidJson": "无效的文件，请选择 .json 种子文件",
"importBadSchema": "种子数据无效，文件可能已损坏或来自不兼容版本",
"importTooManyOps": "种子包含过多操作（最多 20 个），已拒绝导入",
"migrated": "{count} 个种子已升级",
```

Under `"batch"` (inside existing batch object):
```json
"strengthTierLabel": "处理强度",
"strengthTierDesc": "控制操作参数范围和步数",
```

Under `"queue"` (inside existing queue object):
```json
"reorder": "拖拽排序",
"thumbnail": "缩略图",
```

NEW top-level section `"log"` (add after `"batch"` closing brace):
```json
"log": {
  "title": "处理日志",
  "empty": "暂无日志",
  "searchPlaceholder": "搜索文件名/种子...",
  "all": "全部",
  "success": "成功",
  "failure": "失败",
  "noResults": "没有符合条件的记录",
  "duration": "{minutes}分{seconds}秒",
  "status": "状态",
  "outputPath": "输出路径",
  "clearConfirm": "确定清空所有日志？",
  "cleared": "日志已清空",
  "modified": "已修改",
  "unchanged": "未修改",
  "error": "错误"
}
```

New keys for operation type display names:
```json
"operationTypes": {
  "mathOverlay": "数学叠加",
  "pixelShift": "像素平移",
  "frameDrop": "抽帧",
  "gopModify": "GOP修改",
  "metadataErase": "元数据擦除",
  "audioTweak": "音频微调",
  "remux": "重封装",
  "hueRotate": "色相旋转",
  "saturationAdjust": "饱和度调整",
  "brightnessContrast": "亮度对比度",
  "colorBalance": "色彩平衡",
  "filmGrain": "颗粒噪点",
  "gaussianBlur": "高斯模糊",
  "sharpen": "锐化",
  "microRotate": "微旋转",
  "tinyScale": "微缩放",
  "flip": "翻转",
  "solidColorOverlay": "纯色叠加",
  "gradientOverlay": "渐变叠加",
  "watermarkBlend": "水印混合"
}
```

**en.json — new keys to add (exact same key structure, English values):**

Under `"seed"`:
```json
"strengthTier": "Strength Tier",
"strength": {
  "conservative": "Conservative",
  "standard": "Standard",
  "aggressive": "Aggressive"
},
"export": "Export",
"import": "Import",
"exported": "Seed exported",
"imported": "Seed \"{alias}\" imported",
"importFailed": "Import failed: {error}",
"importInvalidJson": "Invalid file. Select a .json seed file.",
"importBadSchema": "Invalid seed data. The file may be corrupted or from an incompatible version.",
"importTooManyOps": "Seed contains too many operations (max 20). Import rejected.",
"migrated": "{count} seeds upgraded",
```

Under `"batch"`:
```json
"strengthTierLabel": "Strength",
"strengthTierDesc": "Controls operation parameter ranges and step count",
```

Under `"queue"`:
```json
"reorder": "Drag to reorder",
"thumbnail": "Thumbnail",
```

NEW `"log"` section:
```json
"log": {
  "title": "Processing Log",
  "empty": "No logs yet",
  "searchPlaceholder": "Search file/seed...",
  "all": "All",
  "success": "Success",
  "failure": "Failure",
  "noResults": "No entries match your filters",
  "duration": "{minutes}m {seconds}s",
  "status": "Status",
  "outputPath": "Output Path",
  "clearConfirm": "Clear all logs?",
  "cleared": "Logs cleared",
  "modified": "Modified",
  "unchanged": "Unchanged",
  "error": "Error"
}
```

NEW `"operationTypes"` section:
```json
"operationTypes": {
  "mathOverlay": "Math Overlay",
  "pixelShift": "Pixel Shift",
  "frameDrop": "Frame Drop",
  "gopModify": "GOP Modify",
  "metadataErase": "Metadata Erase",
  "audioTweak": "Audio Tweak",
  "remux": "Remux",
  "hueRotate": "Hue Rotate",
  "saturationAdjust": "Saturation Adjust",
  "brightnessContrast": "Brightness/Contrast",
  "colorBalance": "Color Balance",
  "filmGrain": "Film Grain",
  "gaussianBlur": "Gaussian Blur",
  "sharpen": "Sharpen",
  "microRotate": "Micro Rotate",
  "tinyScale": "Tiny Scale",
  "flip": "Flip",
  "solidColorOverlay": "Solid Color Overlay",
  "gradientOverlay": "Gradient Overlay",
  "watermarkBlend": "Watermark Blend"
}
```

**Validation:** Check both files are valid JSON with `python3 -c "import json; json.load(open('src/locales/zh-CN.json')); json.load(open('src/locales/en.json')); print('OK')"`.
  </action>

  <verify>
    <automated>cd /Users/ghost/Code/sandwich && python3 -c "import json; zh=json.load(open('src/locales/zh-CN.json')); en=json.load(open('src/locales/en.json')); assert 'log' in zh; assert 'log' in en; assert 'operationTypes' in zh; assert 'hueRotate' in zh.get('operationTypes',{}); assert zh['seed']['strengthTier'] == '强度档位'; assert en['seed']['strengthTier'] == 'Strength Tier'; print('OK: zh keys=' + str(len(zh)) + ', en keys=' + str(len(en)))"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('src/locales/zh-CN.json')); assert d['seed']['strength']['conservative'] == '保守'"` exits 0
    - `python3 -c "import json; d=json.load(open('src/locales/en.json')); assert d['log']['title'] == 'Processing Log'"` exits 0
    - `python3 -c "import json; d=json.load(open('src/locales/zh-CN.json')); assert d['operationTypes']['hueRotate'] == '色相旋转'"` exits 0
    - `python3 -c "import json; d=json.load(open('src/locales/en.json')); assert d['operationTypes']['hueRotate'] == 'Hue Rotate'"` exits 0
    - Both locale files are valid JSON (no trailing commas, no syntax errors)
    - `grep -c "strengthTier" src/locales/en.json` returns at least 1
    - `grep -c "hueRotate" src/locales/zh-CN.json` returns at least 1
  </acceptance_criteria>

  <done>Both locale files contain all new keys. All operation types have display names. Log panel text is complete. Both files are valid JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Pure type definitions and static translation strings; no I/O, no parsing, no execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | No threats in pure type/translation files |
</threat_model>

<verification>
vue-tsc -b exits 0, both locale files are valid JSON, all keys present in both files, new types are importable from their respective modules.
</verification>

<success_criteria>
- TypeScript OperationType union has 20 members
- Seed interface has optional strengthTier
- VideoEntry interface has optional thumbnailBase64 and orderIndex
- ProcessingLogEntry type exists in new types/log.ts
- All ~50 new i18n keys present in both zh-CN.json and en.json
- Both locale files are valid JSON
- vue-tsc -b passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-/06-02-SUMMARY.md`
</output>
