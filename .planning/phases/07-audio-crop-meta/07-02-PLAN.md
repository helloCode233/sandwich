---
phase: 07-audio-crop-meta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/seed.ts
  - src/locales/en.json
  - src/locales/zh-CN.json
autonomous: true
requirements: [D-01, D-04, D-09, D-14, D-17]

must_haves:
  truths:
    - "TypeScript OperationType union has 30 members (20 existing + 10 new)"
    - "'audioTweak' is kept in the union for backward compat"
    - "Seed interface has optional schemaVersion?: number"
    - "10 new i18n keys exist in both en.json and zh-CN.json"
    - "Existing SeedCard NTag display continues to work (renders opType as camelCase string)"
  artifacts:
    - path: "src/types/seed.ts"
      provides: "TypeScript type definitions mirroring Rust models"
      contains: "'audioResample'"
      contains: "'crop'"
      contains: "schemaVersion"
    - path: "src/locales/en.json"
      provides: "English display labels for 10 new operation types"
      contains: "audioResample"
    - path: "src/locales/zh-CN.json"
      provides: "Chinese display labels for 10 new operation types"
      contains: "音频重采样"
  key_links:
    - from: "src/types/seed.ts"
      to: "src/stores/seed.ts"
      via: "import { Seed, OperationType }"
      pattern: "from.*types/seed"
    - from: "src/locales/en.json"
      to: "src/components/seed/SeedCard.vue"
      via: "$t('operationTypes.' + op)"
      pattern: "operationTypes\\."
---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 10 new OperationType union members and schemaVersion to Seed interface</name>
  <files>src/types/seed.ts</files>
  <read_first>src/types/seed.ts (entire file, 43 lines)</read_first>
  <action>
Extend the TypeScript `OperationType` union (lines 19-43) with 10 new string literal members. Insert them AFTER the last existing member (`'watermarkBlend'`) on line 43. Keep `'audioTweak'` on line 25 — do NOT remove it.

Add these 10 members:

```typescript
  // Phase 7: Audio operations (5) — replace audioTweak sub-effects
  | 'audioResample'   // Rust: AudioResample -> camelCase
  | 'audioVolume'     // Rust: AudioVolume
  | 'audioPitch'      // Rust: AudioPitch
  | 'audioEQ'         // Rust: AudioEQ
  | 'audioChannel'    // Rust: AudioChannel
  // Phase 7: Crop (1) — default operation
  | 'crop'            // Rust: Crop
  // Phase 7: Metadata (2) — supplement existing 'metadataErase'
  | 'metadataWrite'   // Rust: MetadataWrite
  | 'metadataSelectiveErase'  // Rust: MetadataSelectiveErase
  // Phase 7: Duration (2)
  | 'videoSpeed'      // Rust: VideoSpeed
  | 'trimEdges'       // Rust: TrimEdges
```

These MUST be inserted BEFORE the closing `;` on line 43. The Rust `#[serde(rename_all = "camelCase")]` attribute produces these exact camelCase names.

Add `schemaVersion` to the `Seed` interface (lines 3-10). Insert AFTER line 9 (`strengthTier?: ...`):

```typescript
  /** Schema version for migration tracking. Phase 6 = 2, Phase 7 = 3. Old seeds default to 0. */
  schemaVersion?: number;
```
</action>
<acceptance_criteria>
- `grep -c "'audioResample'" src/types/seed.ts` returns 1
- `grep -c "'audioVolume'" src/types/seed.ts` returns 1
- `grep -c "'audioPitch'" src/types/seed.ts` returns 1
- `grep -c "'audioEQ'" src/types/seed.ts` returns 1
- `grep -c "'audioChannel'" src/types/seed.ts` returns 1
- `grep -c "'crop'" src/types/seed.ts` returns 1
- `grep -c "'metadataWrite'" src/types/seed.ts` returns 1
- `grep -c "'metadataSelectiveErase'" src/types/seed.ts` returns 1
- `grep -c "'videoSpeed'" src/types/seed.ts` returns 1
- `grep -c "'trimEdges'" src/types/seed.ts` returns 1
- `grep -c "'audioTweak'" src/types/seed.ts` returns 1 (kept)
- `grep 'schemaVersion' src/types/seed.ts` returns a match
- `npx vue-tsc --noEmit src/types/seed.ts` exits 0 (or: `npx tsc --noEmit src/types/seed.ts` exits 0)
</acceptance_criteria>
<verify>
<automated>grep -c "'audioResample'" src/types/seed.ts && grep -c "'crop'" src/types/seed.ts && grep -c "'videoSpeed'" src/types/seed.ts && grep 'schemaVersion' src/types/seed.ts</automated>
</verify>
<done>OperationType union has 30 members (20 existing + 10 new + audioTweak kept). Seed.schemaVersion is optional number. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add 10 new i18n keys to English locale</name>
  <files>src/locales/en.json</files>
  <read_first>src/locales/en.json (lines 159-181, operationTypes section)</read_first>
  <action>
Add the following 10 key-value pairs to the `operationTypes` object in `src/locales/en.json`. Insert them BEFORE the closing `}` of the `operationTypes` block (line 180), AFTER the last existing entry `"watermarkBlend": "Watermark Blend"` (line 179).

Note: line 179 currently ends with `"Watermark Blend"` WITHOUT a trailing comma (it's the last item in the object). You MUST add a comma after line 179 before adding the new entries.

```json
    "audioResample": "Audio Resample",
    "audioVolume": "Audio Volume",
    "audioPitch": "Pitch Shift",
    "audioEQ": "Equalizer",
    "audioChannel": "Channel Op",
    "crop": "Smart Crop",
    "metadataWrite": "Metadata Write",
    "metadataSelectiveErase": "Selective Erase",
    "videoSpeed": "Video Speed",
    "trimEdges": "Trim Edges"
```

The `"audioTweak": "Audio Tweak"` key on line 165 MUST remain (it is NOT being removed or replaced). The new keys are added alongside it.
</action>
<acceptance_criteria>
- `grep '"Audio Resample"' src/locales/en.json` returns a match
- `grep '"Audio Volume"' src/locales/en.json` returns a match
- `grep '"Pitch Shift"' src/locales/en.json` returns a match
- `grep '"Equalizer"' src/locales/en.json` returns a match
- `grep '"Channel Op"' src/locales/en.json` returns a match
- `grep '"Smart Crop"' src/locales/en.json` returns a match
- `grep '"Metadata Write"' src/locales/en.json` returns a match
- `grep '"Selective Erase"' src/locales/en.json` returns a match
- `grep '"Video Speed"' src/locales/en.json` returns a match
- `grep '"Trim Edges"' src/locales/en.json` returns a match
- `grep '"Audio Tweak"' src/locales/en.json` returns a match (existing key preserved)
- `node -e "JSON.parse(require('fs').readFileSync('src/locales/en.json','utf8'))"` exits 0 (valid JSON)
</acceptance_criteria>
<verify>
<automated>node -e "const j = require('./src/locales/en.json'); const keys = Object.keys(j.operationTypes); console.log('Total op type keys:', keys.length); console.log('Has audioResample:', keys.includes('audioResample')); console.log('Has trimEdges:', keys.includes('trimEdges'));"</automated>
</verify>
<done>10 new i18n keys added to en.json operationTypes. Total op type keys = 30. JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 3: Add 10 new i18n keys to Chinese locale</name>
  <files>src/locales/zh-CN.json</files>
  <read_first>src/locales/zh-CN.json (lines 159-181, operationTypes section)</read_first>
  <action>
Add the following 10 key-value pairs to the `operationTypes` object in `src/locales/zh-CN.json`. Insert them BEFORE the closing `}` of the `operationTypes` block (line 180), AFTER the last existing entry `"watermarkBlend": "水印混合"` (line 179).

Add a trailing comma after line 179 before adding:

```json
    "audioResample": "音频重采样",
    "audioVolume": "音频音量",
    "audioPitch": "音调偏移",
    "audioEQ": "均衡器",
    "audioChannel": "声道操作",
    "crop": "智能裁切",
    "metadataWrite": "元数据写入",
    "metadataSelectiveErase": "选择性擦除",
    "videoSpeed": "视频变速",
    "trimEdges": "时长裁剪"
```

The `"audioTweak": "音频微调"` key on line 165 MUST remain.
</action>
<acceptance_criteria>
- `grep '"音频重采样"' src/locales/zh-CN.json` returns a match
- `grep '"音频音量"' src/locales/zh-CN.json` returns a match
- `grep '"音调偏移"' src/locales/zh-CN.json` returns a match
- `grep '"均衡器"' src/locales/zh-CN.json` returns a match
- `grep '"声道操作"' src/locales/zh-CN.json` returns a match
- `grep '"智能裁切"' src/locales/zh-CN.json` returns a match
- `grep '"元数据写入"' src/locales/zh-CN.json` returns a match
- `grep '"选择性擦除"' src/locales/zh-CN.json` returns a match
- `grep '"视频变速"' src/locales/zh-CN.json` returns a match
- `grep '"时长裁剪"' src/locales/zh-CN.json` returns a match
- `grep '"音频微调"' src/locales/zh-CN.json` returns a match (existing key preserved)
- `node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8'))"` exits 0 (valid JSON)
</acceptance_criteria>
<verify>
<automated>node -e "const j = require('./src/locales/zh-CN.json'); const keys = Object.keys(j.operationTypes); console.log('Total op type keys:', keys.length); console.log('Has audioResample:', keys.includes('audioResample')); console.log('Has trimEdges:', keys.includes('trimEdges'));"</automated>
</verify>
<done>10 new i18n keys added to zh-CN.json operationTypes. Total op type keys = 30. JSON is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan modifies TypeScript type definitions and static locale JSON files. No I/O, no external input, no process boundary crossing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | locale JSON files | accept | Locale files are bundled at build time with the application. No runtime modification path. Invalid JSON would be caught by the build step. |
</threat_model>

<verification>
- `npx vue-tsc --noEmit` on the frontend passes (or `npx tsc --noEmit src/types/seed.ts`)
- Both locale files are valid JSON (`node -e "JSON.parse(...)"`)
- OperationType union has all 30 members (grep confirms)
</verification>

<success_criteria>
- [ ] OperationType union in seed.ts has 30 members (grep confirms all 10 new + audioTweak kept)
- [ ] Seed interface has optional schemaVersion?: number
- [ ] en.json has 10 new operationType keys, total 30, valid JSON
- [ ] zh-CN.json has 10 new operationType keys, total 30, valid JSON
- [ ] TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-audio-crop-meta/07-02-SUMMARY.md`
</output>
