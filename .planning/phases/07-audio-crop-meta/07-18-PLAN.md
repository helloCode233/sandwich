---
phase: 07
plan: 18
type: feat
autonomous: true
wave: 1
depends_on: [07-17]
requirements: [FINGERPRINT-08]
---

# Phase 07 Plan 18: Restructure processing pipeline to GPU-first two-pass with CPU-only ops as defaults

## Objective

1. Make all 8 CPU-only video operations default (always included in every seed)
2. Control their strength: CPU ops use conservative tier, GPU ops use seed's main tier
3. Two-pass execution: GPU chain first (with NVENC compression), then CPU chain on intermediate
4. Break backward compatibility with old seeds (schema_version bump)

## Tasks

### Task 1: Pre-inject all CPU-only ops as defaults in generate_seed
- Add Sharpen, MicroRotate, TrimEdges, MathOverlay, PixelShift as conservative-tier defaults
- Pick exactly 1 of 3 overlay types randomly
- Update capacity to step_count + 8
- Update tests

### Task 2: Split operations into GPU/CPU chains in executor
- Add operation grouping logic using is_gpu_capable

### Task 3: Implement two-pass execution in executor
- GPU pass: GPU filters + NVENC with compression → intermediate
- CPU pass: CPU filters on intermediate → final output
- Cleanup intermediate temp file

### Task 4: Bump schema_version to 4

### Task 5: Update tests and verify

## Success Criteria
- `cargo check` passes
- `cargo test --lib` passes (all 91+ tests)
- `cargo fmt` passes
